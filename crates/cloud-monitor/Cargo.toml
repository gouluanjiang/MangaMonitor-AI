[package]
name = "cloud-monitor"
version.workspace = true
edition.workspace = true
license.workspace = true
[dependencies]
state-model.workspace = true
serde_json.workspace = true
tokio.workspace = true
chrono.workspace = true
serde.workspace = true
sha2.workspace = true
# A6.14 private exact media transport; adapter legacy raw transports are test-only.
reqwest.workspace = true
# A6.14B pinned upstream-compatible WEBP decode/re-encode only; this grants no
# inventory, completion, promotion, replacement, deletion, or production authority.
image.workspace = true
rules-core = { path = "../rules-core" }
jm-adapter = { path = "../jm-adapter" }
pica-adapter = { path = "../pica-adapter" }
