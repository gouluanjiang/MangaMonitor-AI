# Author query eligibility and inventory contention repair

## Problem and behavior

The followed-author batch used raw keyword queries for every saved name. A missing-author placeholder and a one-letter credit can therefore read hundreds of keyword-result pages before UI attribution separates unrelated works. A long result set alone does not prove that a credited author is invalid. The private import audit confirmed a placeholder omission and a real short credit whose explicit full circle/author signature had been reduced to the initial.

Runtime query eligibility now rejects an exact normalized placeholder set and marks single ASCII letter/digit author queries too broad. Fullwidth ASCII, surrounding whitespace and ASCII case fold consistently. Valid one-character CJK names, longer pen names and complete circle/author signatures remain eligible. The generic work-keyword and direct work-ID modes are unchanged; valid authors still read every returned page, with no arbitrary new page cutoff.

Each blocked discovery scope is partial with an actionable local reason and zero requests for that attempt; other authors continue. No new checkpoint is granted. Older blocked ranges project as partial with no usable baseline, and exclusively blocked record associations are hidden from author results without deleting stored history. Records shared with valid authors remain available. Stored document validators remain compatible. Adding placeholder follows is rejected, while removal and real short-name follows remain supported. Both standalone author-search UI paths apply the same rule before source requests. Complete-catalog counts exclude invalidated scopes despite retained historical timestamps.

## Inventory status

During the reported run the UI temporarily displayed all inventory states as unknown, and later recovered. Code review found that a transient private-store `BUSY` could leave inventory unavailable until another explicit refresh; there is no captured error proving that this particular screenshot was caused by `BUSY`.

Native inventory reads now retry only exact `BUSY` up to five attempts with 20 ms gaps, on the existing blocking worker. Persistent contention and real read/root errors still report unknown. The author pages explain the failed inventory check and point to their existing refresh button. Discovery polling does not repeatedly rescan inventory, and no long reconnect mechanism is introduced.

## Verification and private correction

Synthetic regressions cover normalized query eligibility, mixed/fully blocked batches, zero source requests for blocked scopes, old-document projection and preserved records, follow removal, unchanged generic keyword searches, and a valid author with 125 pages. Native regressions use a real local lock, cover bounded contention and recovery, and verify corruption is returned immediately without modifying files. UI regressions cover actionable skipped-range reasons, truthful completeness, rejected standalone/source author entry, unchanged explicit keyword mode, and inventory recovery.

Final head `1875d867f86ab8326056bd18fa7decd0f81f70c8`, test merge `f6ef6f65f1c6fa7d0ee37abdc706695307937257`, passed frontend `35449788124` (154 logic / 121 Chromium), baseline `35449788123`, and desktop `35449788104` (including 72 account tests, 37 native IPC tests, credential checks, Clippy, the release build and actual Windows WebView startup/restart). Formal suites and builds ran only in GitHub Actions. An initial synthetic-test compile error was corrected without changing the production snapshot type or weakening the tested rejection.

The final EXE is delivered at `Documents/Codex/MangaMonitor-Dev-20260919-1875d86/mangamonitor-workbench-preview.exe`, with SHA-256 `2b06870a72f1df2810ebdc89b79f5eb9da0a87fa398bc96fed4c66be077a022e`. Artifact digest, ZIP CRC, PE architecture and embedded revision have been verified. Bounded final native delivery verification has passed; user acceptance remains pending. Version remains 0.3.4, with no installer, release, merge or production enablement. Both existing Dev shortcuts were backed up and now point to the final EXE; old executables remain available.

The reported batch was stopped through its normal UI and saved results backed up. A bounded real-source query of the complete circle/author signature did not complete on JM. A subsequent query using the explicitly evidenced circle name completed on both sources, so the private correction uses that circle name; it does not claim successful two-source coverage for the full signature or infer a new identity. The revised private correction was applied atomically after backing up the original library, following, download and discovery documents. The resulting following document exactly matches the prepared correction, while library and download documents remain byte-identical.

On the final native EXE, the corrected circle query completed in one page on each source. Standalone author searches for the guarded input shapes `N/A` and `P` visibly reported their distinct local blocking reasons. Refreshing inventory retained known ownership states with no unknown entries. A cold restart through the updated desktop shortcut passed: saved history remained available, inventory remained known, and the all-author page retained an honest incomplete-range indication, the default unowned filter and an idle check that requires a user click. No entire-following scan or media download was performed for acceptance. These observations do not establish user acceptance, complete author coverage or a live reproduction of the `BUSY` condition.

The final private preservation audit passed: every original discovered work key and its metadata remain saved, and unrelated source ranges remain unchanged. Normal reconciliation removed only the old ranges belonging to the removed or replaced query labels; raw record history remains saved, and blocked query associations are excluded at runtime rather than physically purged. Private names, counts, before/after documents, query evidence and `final-profile-preservation-aggregate.private.json` stay outside Git in `Documents/Codex/MangaMonitor-author-query-repair-20260919`.

The canonical checkout is fast-forwarded to the final head. The previous evidence edits remain backed up, with preserved stash `f3059d16849e041b631b41a7c53312e3d1732249`. Do not delete a real author merely because their name is short, rerun the entire followed list for acceptance, erase discovered history, or modify manga/download data. These final evidence-only document edits do not require a separate CI rebuild.
