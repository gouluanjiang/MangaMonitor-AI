# Active development handoff

Updated 2026-09-22. This file is the current continuation record. The previous handoff, including its six final local evidence edits, is preserved in [the historical handoff](DEVELOPMENT_HANDOFF_ARCHIVE_2026-09-15.md). Historical pending/current statements do not override this file or the latest user instruction.

## Active batch: source-specific author queries and reviewed credit policies

The user approved implementation after discussion of author retrieval accuracy and query spelling. See [the current contract](AUTHOR_QUERY_POLICIES_2026-09-22.md). Preserve followed display names, the completed list correction and all library/download state. Evidence-backed query words and attribution aliases are separate, scoped by account/source/author. Only changed query scopes invalidate their remote baselines; aliases reclassify saved records offline. Do not infer identities, restore removed names, merge follows or claim a smaller keyword count proves complete author coverage.

The implementation worktree is `Documents/Codex/MangaMonitor-pc-zip-B-20260913`; private per-name evidence, reviewed policy candidates and bounded validation plans are under `Documents/Codex/MangaMonitor-author-query-20260922`. Existing final evidence edits were preserved before this batch. Implementation and targeted synthetic regression are in progress; final CI, private import, executable delivery and bounded native acceptance are not yet claimed. Formal checks/builds stay in CI. No all-author replay, manga download, installer, release, PR merge or version bump is part of this batch.

## Overall position and remaining work, 2026-09-22

V1 product experience was accepted by the user on 2026-09-17. The project is now in post-V1 maintenance and source-search quality improvement, not an unfinished V1 feature rollout. The current code/delivery is `86f7b4c`, Dev 0.3.4. Fresh GitHub metadata confirms PR #19 remains open/draft/unmerged and its three latest workflow runs passed. Formal versioning, installer/release and merge decisions remain separate from product acceptance.

Completed capabilities include two-source accounts/sessions/favorites and session cover caching; manual author search and saved updates with complete pagination, attribution separation and ownership filtering; all-followed-author incremental/full/unfinished checks; both sources' rankings; bounded batch ZIP download and receipt/file-based admission; the PC library; work/admission dates and sorting; settings and diagnostics. The authorized old-library review and followed-name correction have also completed their applied scope. Do not reopen older completed batches.

Remaining maintenance: evidenced author-query/attribution risks and uncertain credits; original source query spelling versus comparison normalization; broad-query first-scan request cost and measured performance; the recurring recoverable initial local-read BUSY observation; and bounded real-source verification of the latest malformed-item isolation change. List inspection, all-page completion and native following-list checks do not certify exhaustive author bibliographies or that latest specific regression. History date gaps and residual old-library source-ID gaps are optional data improvements, not mandatory re-imports. Query narrowing, source concurrency and cross-run page-number resume are unimplemented options, not already-approved feature promises.

Only the reader and in-app updater remain separate post-V1 feature discussions. Runtime identity matching/manual cross-source association, dislike, translation/version inference or replacement, phone lists, classification booklists, PDF/CBZ export, extended reconnect/recovery features, full remote-folder management, tray/autostart, standalone TXT ID import, and cloud continuous monitoring/inventory callbacks remain cancelled. `production_enabled=false` is an execution boundary, not a missing desktop V1 product feature. The full private Chinese inventory and prioritized roadmap are at `Documents/Codex/MangaMonitor-PROJECT_STATUS_2026-09-22.md`; private counts and names stay out of Git. This status consolidation starts no scan, download, release or development batch.

## Current follow-up: second full name audit and approved list correction complete

The user requested removal of the previously reported problematic/review query-name subset and an individual second review of every remaining name, then directed that list changes happen together after that review. All second-round rows are now saved with individual evidence, and the combined coverage check confirms no missing or duplicated names. One additional confirmed concatenated coauthor credit was found. Other query/attribution risks remain differentiated from invalid names; do not turn uncertain or zero-result names into deletion candidates.

Private complete results, reports and the exact changes are under `Documents/Codex/MangaMonitor-followed-author-audit-20260922/round2`. The user explicitly authorized resumed Computer Use and the combined correction. Native inspection confirmed the existing scan complete and downloads idle; the app was normally closed, all root profile JSON was backed up, and the exact following-only replacement was applied under the existing store lock after hash and field-delta checks. The selected subset was removed and the newly confirmed concatenation corrected while preserving the coauthor already followed. Independent verification confirmed the intended list/order delta and byte-identical unrelated profile JSON, including discovery/library/download data. Manga files were not changed.

The same verified executable was reopened and both source following views loaded the corrected counts and credits; the local library still loaded normally. One startup busy read settled after an ordinary local following refresh. No new all-author scan, manga download, source-record deletion or additional product code/build/CI was performed. The app is left open on the corrected Pica following list. Full per-name review does not certify exhaustive live-source search behavior: the separately documented query risks and attribution uncertainties remain preserved for later investigation. Execution, native verification and independent preservation receipts are in the private round2 directory. Do not replay the applied combined plan or use the older rename/merge or removal-only plans. Previous Escape/auto-review blocks are resolved by the explicit later user permission; no authorization is currently pending for this completed operation.

## Previous follow-up: all followed-author names audited

The user requested a fresh check of the entire current followed-author list after the broad-query spelling defect below. A read-only, full-list audit is complete: current following names were checked against original import provenance, explicit source author labels and the hash-verified saved discovery catalog. Separate spelling and semantic reviews distinguish compatibility folding, ordinary case/composition/whitespace differences, invalid imported roles, truncated names, real aliases and legitimate short/common-word credits. Bounded anonymous JM first-page comparisons confirmed additional query-range differences, while other compatibility variants returned matching first-page IDs and totals. Those first-page probes are not a complete two-source network recheck; no all-author replay or fresh Pica query was started.

The private audit, per-name table, source-response comparisons and preservation receipt are under `Documents/Codex/MangaMonitor-followed-author-audit-20260922`, led by `全部关注作者复核报告.md`. All real names, IDs, counts and library provenance remain outside Git. Following remained byte-identical; the user's running scan, profile, library, UI and application code were not modified. Some narrower source queries omit previously confirmed legitimate works, and source-specific spacing is explicitly evidenced, so do not replace names globally, drop historical results, delete zero-result/short-name authors, or treat fewer keyword hits as sufficient proof of a good replacement. No fixes are claimed applied. This audit is separate from native/user acceptance of the delivered item-isolation build. Evidence-only notes require no new build or CI.

## Previous follow-up: author query spelling during user acceptance

The user is now performing acceptance and reports a JM author range approaching one hundred pages. Read-only inspection of the running scan's committed, hash-verified directory and three bounded anonymous first-page requests confirmed a query-spelling defect: the earlier one-time author import saved an NFKC comparison form as the actual website query, folding a source's fullwidth punctuation into ASCII. The two spellings produce radically different source results; the broad query also misses some explicitly credited works returned by the source's original spelling. The currently delivered runtime preserves its input and the latest item-isolation change did not widen the query. Do not blame the author or remove the follow, infer undocumented source operators, or impose a silent page cutoff.

Detailed names, counts, IDs, original-alias import evidence and proposed correction are private under `Documents/Codex/MangaMonitor-item-isolation-20260922/作者搜索波浪号差异排查.md` and its timestamped diagnostic files. No application code/profile changes or UI interactions were made in this investigation; the user's active scan was not interrupted. The next correction should preserve original source query spelling separately from normalized comparison keys, review affected imported names against explicit saved evidence, and invalidate/rebuild only changed-query baselines while retaining historical normal records. Do not equate this diagnosis with a completed repair or with acceptance of the new build. No new CI is needed for these evidence-only notes.

## Current development: source-list item isolation

The user approved the proposed extension after asking whether a malformed row can hide normal comics. Per-item isolation now spans source lists and their consumers, with honest raw-row pagination, an explicit issue summary/list and persisted author-scope uncertainty. Good records and source-author evidence, strict detail/download preparation and validated pagination envelopes remain preserved. See [the contract and final evidence](LISTING_ITEM_ISOLATION_2026-09-22.md). Formal suites and builds remain CI-only. Before-state documents, cache copies and the previous final reports are preserved privately under `Documents/Codex/MangaMonitor-item-isolation-20260922`.

Final head `86f7b4cd9ac2690f0c8834dc80d183e928aa719d`, test merge `07ff073fa85b768c9d564dad6cbbbfb83ca5d718`, passed frontend `35691141971` (169 logic / 149 Chromium), baseline `35691141968`, and desktop `35691141989` (86 account / 70 source / 38 native IPC, Clippy, build and Windows WebView startup/restart). Four new synthetic UI screenshots were inspected, with the final author-date sentence corrected and revalidated. No formal suite was duplicated locally.

The final EXE is delivered at `Documents/Codex/MangaMonitor-Dev-20260922-86f7b4c`, SHA-256 `4771577ecf66dbbcbd19335fd2b959537050b28a49e6430b0db0c4fe86c22488`; artifact digest/CRC/x64 PE/embedded revision passed. Both Dev shortcuts now point to it, with old shortcuts and executables retained. Canonical is at this head. Prior final evidence is preserved in stash `6b983a57e7f19015358bf651c1af38a34c161839`; preserve it and earlier stashes. Final evidence-only edits remain local for the next necessary code push. Version remains 0.3.4, PR #19 draft/unmerged, production disabled.

At delivery, native verification was deferred by the user's explicit choice “暂不进行实机核验” after the window tool returned an empty WebView tree, failed activation and captured another program. The user subsequently resumed window operations for the list correction described above, and startup/library/following checks passed. That permission is no longer pending, but those checks do not replace this change's bounded real-source regression. The remaining specific scope is one relevant author, not all followed authors. The delivery preservation audit and later list-correction receipts remain separate evidence. Engineering/delivery work is complete; a closed-loop real-source verification record for this latest isolation change remains outstanding. Prior final V1 acceptance stays accepted.

## Previous repair: JM listing metadata blocks the last author scope

The user reports one remaining JM author scope failing after one page with a source-response-format error. Two bounded public search-page reads and one exact-ID detail read identified a valid-ID record with blank title/author metadata. The strict list parser rejects its blank title, so the whole subsequent page fails. This is a source metadata gap plus a client isolation defect, not an invalid author label or evidence of deletion. See [the bounded correction and verification contract](JM_MISSING_LISTING_METADATA_2026-09-20.md). Private evidence and before-state backups are under `Documents/Codex/MangaMonitor-jm-last-author-20260920`.

Implementation and independent review are complete. JM listing IDs and pagination counts are preserved with an explicit missing-information label and no cover descriptor, while detail/download preparation remains strict and authorship is never inferred. Focused source and author-flow regressions cover later-page continuation, metadata guards, preserved counts and exclusion from author bulk actions. No all-author replay, extra per-work requests, Pica changes, media download or library mutation is included. Both prior final evidence document edits are backed up and included with this necessary code push.

Head `4ca34bfe8f15db3cff5621cfaf7a3a16fe737c9b`, test merge `239fca04ce5b400c02bc409b68a22abd05184f21`, passed frontend `35518925424` (164 logic / 145 Chromium), baseline `35518925486` (64 source tests), and desktop `35518925418` (79 account / 38 native IPC, Clippy, build and actual Windows WebView startup/restart). Formal suites were not duplicated locally. A separate offline replay of the captured source pages through current frontend validation, pagination and author classification preserved all source identifiers, page totals and the one explicit missing-metadata record. That replay is not native acceptance and does not execute the Rust parser; the private result states this boundary.

The verified EXE is delivered at `Documents/Codex/MangaMonitor-Dev-20260920-4ca34bf`, SHA-256 `8c8f0b482ee32ad8696699bcf544c1cedac979e2e578248abca6c0c1e2e2def7`. Artifact digest, CRC, x64 PE and embedded revision are verified. The previous idle app was normally closed, the new EXE launched, and both existing Dev shortcuts were backed up and repointed; old programs remain. Native startup and existing library display passed. After the user reconnected Pica, bounded native validation passed: exactly one unfinished JM pair was retried, all source pages completed, the incomplete list cleared and every followed source scope now has a complete baseline. A transient progress-poll BUSY recovered automatically. The missing metadata record remains explicitly represented and excluded from the author classification. No all-author replay, completed-Pica recheck or media download occurred. This delivery includes the prior date-coverage explanatory UI too.

Independent before/after semantic audit passed: every original work key/order and query association remains, unrelated records/source ranges and the same author's Pica scope are unchanged, and all eight documents/cache files outside the changed discovery checkpoint/manifest are byte-identical. The single scan and selected JM range explain every added/changed discovery record, with no old author metadata or valid date lost. Private original captures, offline replay, native observation, shortcut/artifact receipts and preservation reports are under the repair evidence directory. The authenticated app is left open and idle on all followed authors, both sources, unowned results and website-update descending order. Do not close it to repeat accepted validation.

Canonical is at this head on `codex/local-workbench-preview`; its prior final reports were verified before retention in stash `b51a94565f5f23ba8dbf33ee4d9d119ee56d3a41`. Preserve it and earlier stashes, saved history and deliveries. Final evidence-only edits are kept local for the next necessary code push. Version remains 0.3.4, PR #19 draft/unmerged and production disabled. All required engineering and bounded authenticated native verification are complete; user acceptance remains separate.

## Previous repair: date coverage in saved author updates

The user reported that dates and both update-time directions appear ineffective in the author-updates page. Read-only inspection confirmed that the active pre-feature saved catalog has no work-update dates, and the saved favorites caches contain no recoverable original update fields. Both directions therefore correctly retain the original order of equally unknown dates. A temporary author search obtains dates only in its own in-memory result; it does not populate the persistent author-update catalog. Incremental checks refresh encountered pages, not every historical page. The previous bounded acceptance below proved fresh search ordering, but did not prove date coverage in saved author updates; that acceptance gap is now explicit.

The correction reports valid and unknown update-date counts for the current visible result set, explains all-unknown and partial-date ordering, and retains incomplete-pagination coverage. Missing dates in updates are accompanied by a scoped path: select one author and use the existing full recheck to reread both sources' pages. A normal local refresh does not fetch website dates. No automatic all-author replay, per-work detail fan-out, invented fallback date, changed ownership rule or source query behavior is introduced. Tests cover legacy all-unknown order, filters, and saved dates arriving on refresh while preserving the chosen sort direction. Private diagnostics and pre-repair report copies are under the work-dates evidence directory's `author-update-date-diagnostic` subdirectory. Do not claim that this UI correction itself fills the old catalog.

Follow-up head `54f2ffb9ba884ddf658b6eccfddf7718f7ca2b48`, test merge `c4c74315bc441e510ddf6f28359137fbf98e0aa8`, passed frontend `35517271412` (163 logic / 143 Chromium), baseline `35517271405`, and desktop `35517271429` including native IPC, Clippy, build and actual Windows WebView startup/restart. The new missing-date screenshot passed visual review. Formal suites were not duplicated locally. Canonical was safely fast-forwarded after verifying the two prior report edits against their private backups and retaining them in stash `09963e8857417059ba114d975fb372c10ba074fc`.

Bounded native diagnosis on the still-running prior verified EXE performed a full recheck for exactly one previously approved author across two sources. Available dates are now saved, both time directions passed against visible real timestamps, and owned/unowned counts stayed unchanged. Independent semantic audit verified every original work key, unrelated source range, library/following/download document and favorite cache was preserved. Only the selected source queries' records and scan evidence changed. No all-author replay or media download occurred. This is native evidence for the existing persistence/sort path, not native acceptance of the new explanatory UI. Preserve the current authenticated session, default descending order and selected author's unowned view. Other old author catalogs still need explicit rechecks to acquire available dates; there is no silent global backfill.

The corrected EXE is staged at `Documents/Codex/MangaMonitor-Dev-20260920-54f2ffb`, SHA-256 `ca935a04162c2d8b6691856702a65dabef8150dcf2e98bd8587464fbde78f85e`; artifact digest, CRC, x64 PE and embedded revision are verified. Both existing Dev shortcuts point to this delivery, with the old shortcuts and EXE retained. The currently running `eb1c592` window was deliberately retained to preserve the authenticated session. New explanatory UI therefore takes effect on the next normal launch; do not say that the running window has hot-updated or that the new UI passed private native acceptance. Required CI and the bounded saved-directory diagnosis are complete, user acceptance remains pending. Version remains 0.3.4, PR #19 draft/unmerged, production disabled. Final evidence-only report edits are synchronized to canonical and kept local for the next necessary code push.

## Previous development: work dates and sorting

The user approved source update dates for search/author updates, downloaded-version update dates plus admission dates for the local library, both sort directions and unknown historical dates placed last. Implementation and CI are complete; see [the contract and final evidence](WORK_DATES_AND_SORTING_2026-09-20.md). Preserve the already accepted source/ownership/author rules, pagination traversal, saved records, original admission dates and current package bytes. Do not fill historical local version dates from current website values or trigger mass source queries for this feature. Private backups and delivery evidence belong under `Documents/Codex/MangaMonitor-work-dates-20260920`.

Final head `eb1c592bcb6c1be3998709704d6f29f059cbc3fb`, merge `4e5a6719f766bcf0ab0e3fea9991583369cd89f7`, passed frontend `35514819949` (163 logic / 141 Chromium), desktop `35514819885` (79 account / 38 native IPC plus remaining checks and actual Windows WebView restart), and baseline `35514819889`. The verified EXE is delivered at `Documents/Codex/MangaMonitor-Dev-20260920-eb1c592`, SHA-256 `0f57b76f328318ffb81cbd181c862645ba79e64fb661a7227d7364b1ca88f625`. Both Dev shortcuts now point to it; previous programs and shortcut backups remain. Native startup/library date display, both admission directions and the version-date entry passed. After the user reconnected Pica, the remaining authenticated verification passed: saved author results/ownership, honest incomplete coverage, independent remembered sorting across library/search/updates, and one previously approved author's complete two-source query with actual dates ordered in both directions and unchanged filter counts. The authenticated app is left on unowned search results newest first, with library admission and update defaults restored. Preserve the Pica session; no second cold restart is needed.

No all-author replay, library rescan or media download occurred. The ad-hoc author search is in-memory and did not alter saved author updates. An independent final audit confirmed the seven selected store documents and three cache documents are byte-identical, including the active checkpoint and zero-patch manifest. Bounded native validation is complete; private details remain in the batch directory's final native and preservation reports. Do not repeat accepted checks or interpret this as a new user acceptance.

Canonical checkout is fast-forwarded to this head; preexisting final-report changes were included with implementation and also retained in stash `5db0e9f54d044dd012d34931bdf133197e52a08d`. Preserve earlier stashes and deliveries too. Final evidence-only report updates remain local for the next necessary push. Historical version dates require trustworthy ZIP metadata and an ordinary rescan; existing saved source dates may remain unknown until normal refresh. User acceptance is pending. Version remains 0.3.4, PR #19 stays draft/unmerged and production remains disabled.

## Previous development: large author catalog optimization

The user authorized local catalog scaling, lightweight progress and a new only-unfinished check after the all-author scan reached the shared raw-record capacity. Implementation and final CI are complete; see [the batch contract and evidence](AUTHOR_CATALOG_SCALE_2026-09-20.md). Preserve all prior author records and complete checkpoints, the original JSON, existing following/download/library documents and the previous repair's read-error/session guards. Per-page journal transactions replace full-document writes; the default result view loads explicit author matches and other keyword results remain available on demand. Progress polling carries no work records. Individual failed or unstarted author/source pairs can be retried without revisiting completed pairs. Limited same-page retries do not change pagination or ownership semantics.

Final head `8c1a8927a82082bbc50b29f1ea0b28e6c3ab3aeb`, test merge `bd059a06221dfb69846ef259e1f5b3d6abf058cf`, passed frontend `35511468960` (158 logic / 137 Chromium), desktop `35511468937` (77 account / 37 native IPC plus remaining checks, build and actual Windows WebView restart), and baseline `35511468959`. Final EXE SHA-256 is `f263fb08bb8090e3a035f026947862ba0a524943e5d700646d5ef18e31d44bec`; delivered directory is `Documents/Codex/MangaMonitor-Dev-20260920-8c1a892`. The isolated existing-catalog diagnostic passed lossless page-commit/checkpoint/reopen and legacy-byte preservation. Real data, timings and before-state backups remain private under `Documents/Codex/MangaMonitor-author-scale-20260920`; do not turn local IO timings into source-search speedup claims. Native startup and existing local-library display passed. After the user reconnected both accounts, saved author results/ownership, the unfinished-count button, on-demand keyword results, return to author results and manual refresh passed bounded native verification. The final idle view shows unowned works. Four live data documents remain byte-identical. No scan or download was started, and no second cold restart followed login. Both Dev shortcuts now point to the verified EXE, with old shortcuts/executables retained and the authenticated window left open. User acceptance and total real-source timing remain pending.

Canonical checkout is fast-forwarded to the final head. The two prior final evidence document edits are included, and their separate preserved stash is `d51d416992e38edc944a6029e41b60ba36b85793`. Preserve it and earlier stashes/deliveries. Final evidence-only report edits remain local for the next necessary code push. No all-author replay, media download or author deletion is needed for delivery validation. Query narrowing, cross-run page continuation and two-source concurrency are not bundled into this change. Version stays 0.3.4, PR #19 stays draft/unmerged and production remains disabled.

## Previous repair: author progress stops refreshing after a read failure

Read-only diagnosis confirmed that the frozen counter did not represent a stopped backend: later scopes continued being committed and the displayed scope had already completed. A single snapshot read error disabled the frontend polling effect while leaving its old checking snapshot visible. The user now authorized the repair. See [the progress refresh contract](AUTHOR_PROGRESS_REFRESH_FIX_2026-09-20.md). The exact original read error remains unproven; do not assert that a source, document limit or `BUSY` caused it solely from the generic message.

The change separates progress-read and action failures, labels stale progress, retries only exact `BUSY` reads with a bounded budget, and keeps manual refresh/stop controls available. It never restarts author searches or polls while the page is inactive. Source pagination, backend storage and private-library identity rules remain unchanged. Independent review and final CI passed at head `f2dbf4cf06897c1cfe1197f9d0500568e66cd769`, test merge `3c6578e23cc4214b7054851962e689dc56166a54`: frontend `35499348508` (154 logic / 132 Chromium), desktop `35499348588` (72 account / 37 native IPC, credential checks, Clippy, build, actual Windows WebView startup/restart), baseline `35499348559`. Two earlier failures corrected only the new cancellation test's expected error contract and asynchronous wait; production code was unchanged by those corrections.

The exact final EXE was digest/CRC/PE/embedded-revision verified at `Documents/Codex/MangaMonitor-Dev-20260920-f2dbf4c/mangamonitor-workbench-preview.exe`, SHA-256 `0c9f056afe13df4b50f837a5e3a05b1889abfb7e98d6efebeb6c91105b7f2b04`. After the user reconnected both accounts, bounded native acceptance passed: existing author results and known ownership counts load, manual refresh settles correctly, and page reentry preserves those results. The unfinished scope remains explicitly incomplete without an obsolete running counter/stop button. Initial startup and the local-library display passed. Four private store documents remain byte-identical to the pre-repair baseline, also confirmed by an independent read-only audit of current files and backups.

Both existing Dev shortcuts now point to the verified final EXE, with old shortcut backups and executables retained. The current authenticated app remains open on the idle author-updates page. Pica is connected for the current session only; preserve that session rather than closing the app to repeat login. No second local cold restart was performed after reconnection. Windows restart/persistence verification is the synthetic CI result, not fresh private-author cold-start acceptance. No all-author replay, media download or native fault injection occurred. User acceptance of this fix remains pending; no further engineering change or required validation remains for this bounded delivery.

Before this repair, the app was no longer running. Preserve the latest interrupted discovery state and the other private documents; no all-author replay is authorized merely for acceptance. Previous final query-guard evidence edits and the current private documents are backed up under `Documents/Codex/MangaMonitor-progress-refresh-20260920`. Those preexisting report changes were included with the code push; the preserved canonical-report stash is `1bff34719106436ba816c829701f8531c3a154f9`. Preserve it, earlier stashes and executable deliveries. Canonical checkout was fast-forwarded to the final head; final evidence-only report edits are synchronized there and remain local for the next necessary code push. PR #19 stays draft/unmerged, version stays 0.3.4 and production remains disabled.

## Previous repair: placeholder and broad author queries

The user reported excessive pages for a missing-author placeholder and a one-letter credit during the all-followed check. The run was stopped through the app, preserving committed results. Private audit distinguishes the invalid placeholder from the genuine short credit; it does not infer that high page counts make an author invalid. See [the repair contract](AUTHOR_QUERY_GUARD_FIX_2026-09-19.md). Code now blocks those unsafe author queries before IO, reports partial local reasons, preserves history and normal full-pagination behavior, and covers both author search entries. A separately found transient inventory-read `BUSY` recovery gap receives bounded local retry and an explanatory UI message; the screenshot's exact transient cause was not logged.

Final head `1875d867f86ab8326056bd18fa7decd0f81f70c8`, test merge `f6ef6f65f1c6fa7d0ee37abdc706695307937257`, passed frontend `35449788124` (154 logic / 121 Chromium), baseline `35449788123`, and desktop `35449788104` (72 account tests, 37 native IPC tests, credential checks, Clippy, build and actual Windows WebView startup/restart). Formal suites and builds ran only in CI. The final EXE is delivered at `Documents/Codex/MangaMonitor-Dev-20260919-1875d86/mangamonitor-workbench-preview.exe`; SHA-256 `2b06870a72f1df2810ebdc89b79f5eb9da0a87fa398bc96fed4c66be077a022e`. Artifact digest, CRC, PE architecture and embedded revision are verified. Final bounded native delivery verification passed; user acceptance remains pending. Both existing Dev shortcuts were backed up and now point to this EXE, with old executables retained.

A bounded real-source check of the complete circle/author signature did not complete on JM. The explicitly evidenced circle name subsequently completed on both sources; the revised private correction therefore selects that circle name. The correction was atomically applied after backing up the four original private documents. Following exactly matches the prepared result, and library/download documents remain byte-identical. No successful two-source coverage is claimed for the full signature.

Final native verification passed for the corrected circle query, with one page on each source, and for distinct blocked-input messages in standalone author searches. Inventory refresh produced known states with no unknown entries. A cold restart through the updated desktop shortcut preserved saved history and known inventory; the all-author page is left with truthful incomplete coverage, the default unowned filter and a manual idle check. No all-author replay or media download occurred. The private preservation audit retained every original discovered work key and its metadata, with unrelated ranges unchanged. Normal reconciliation removed only the old ranges for removed or replaced query labels; raw history remains stored and blocked associations are excluded at runtime, not physically purged. Private names, counts and evidence remain in the repair directory, including the passing `final-profile-preservation-aggregate.private.json`. Do not claim user acceptance, complete all-author coverage or a live reproduction of `BUSY`, and do not replay the applied correction.

The canonical checkout is fast-forwarded to the final head. Preserve the prior two final evidence edits backed up in `Documents/Codex/MangaMonitor-author-query-repair-20260919/preexisting-reports`, their preserved stash `f3059d16849e041b631b41a7c53312e3d1732249`, and the private before-repair documents. Version remains 0.3.4, no installer/release/merge or production enablement. No all-author replay, media download or library mutation is included in repair acceptance. Final evidence-only document edits remain local for the next necessary code push; they do not require a separate rebuild.

## Completed post-V1 work: manual incremental author checks

The user approved a one-click all-followed-author check with a complete initial catalog and later incremental checks. The implementation preserves old unowned results, defaults the page to unowned items and adds an explicit full recheck. Existing ad-hoc author searches remain complete. See [the incremental check contract](AUTHOR_INCREMENTAL_CHECKS_2026-09-19.md) for checkpoint conditions, full-scan fallback, time/range semantics and limitations. It does not add background monitoring or automatic downloads.

Final head `3b936a79fffe57581ae2e17f8a73a010bfb6d7aa`, merge `0962ccb4969691561fc1a9c50772fa5a7423cf5a`, passed frontend `35446655889` (152 logic / 117 Chromium), desktop `35446655877` (including 66 account tests, 8 discovery-storage tests, 34 native IPC and actual Windows WebView startup/restart), and baseline `35446655883`. Formal suites and builds ran only in CI. Two existing UI tests now await their real asynchronous boundaries; product assertions remain unchanged.

The digest/CRC/PE/revision-verified executable is delivered separately at `Documents/Codex/MangaMonitor-Dev-20260919-3b936a7`; EXE SHA-256 `cb5999f2a3f2ba671c2d4e179069e1787b2cc0abf24040be719d4d267373f9dd`. Bounded native acceptance passed on one existing followed author across both sources: first complete pagination, reduced repeat incremental requests with retained records and last-full timestamps, explicit full recheck, and saved-state recovery after restart. Library, following and download documents remained byte-identical; discovery did not change on restart. No all-author live scan or media download occurred. The page is left on all followed authors with the unowned filter and an honest incomplete-range indication for unchecked authors. Both existing Dev shortcuts point to the new EXE. This is agent verification, not a new user-acceptance claim or a full-following performance benchmark.

The preexisting two report edits were backed up outside Git under `Documents/Codex/MangaMonitor-author-incremental-20260919/preexisting-reports` and included with the code change; their Git stash is also preserved. Private before/after evidence, the Chinese report, shortcut backups and original private documents remain in that task directory. New optional discovery fields load older documents, but rollback to an older EXE requires the compatible retained backup. Old executables remain. Version stays 0.3.4, no new installer/release/merge, draft PR #19, production disabled. Final evidence-only edits to this handoff and the contract are intentionally local, mirrored in the worktree and canonical checkout for the next necessary code push. Do not rerun the passed acceptance, apply prior library imports or launch all authors merely to close documentation.

## Completed project 3: reviewed old-library registration

### Library authors added to local following, 2026-09-19

The user explicitly requested extraction of all current library authors and addition to the application's followed-author lists. A private one-time review normalized explicit authorship, repaired known tokenizer fragments, separated named coauthors, and retained evidenced source spelling variants and circle-only credits. Unknown credits and generic/translation/character noise were not guessed into author identities. Complete per-name evidence, unresolved authorship notes and the plain-text list remain outside Git in `Documents/Codex/MangaMonitor-follow-library-authors-20260919`.

The prepared names were added to both existing account scopes after confirming the saved scope relationship, closing the app and backing up the private documents. The following document alone was updated with schema/name/capacity validation, exact prior hashes, a revision increment, the existing store lock and an atomic replacement retaining the original. Existing follows and work follows were preserved. The prior library, downloads and saved discovery documents remained byte-identical.

The unchanged verified executable restarted, both source following screens displayed the expected complete counts, and the author-update entry reflected the newly unchecked range while retaining its old results. No all-author check, media download, website follow mutation or runtime matching change was performed. The known initial local-following busy indication cleared through normal refresh; its cause is not claimed fixed. Some works lack reliable creator credits, and query names include necessary aliases rather than representing a count of distinct people. Do not replay this applied import or start a full-source scan solely to reproduce its evidence. No code, build, version, installer, release or production setting changed.

### User-directed file cleanup and local acceptance completed, 2026-09-19

The user authorized deletion of the identified empty ZIPs, accepted the designated creator-only files as local library works without further identifier lookup, and delegated the remaining file-treatment choices. Only the exact freshly verified empty archives were deleted. The creator-only works were already locally indexed; their accepted status does not invent website IDs or mark unrelated online results owned.

Archives containing zero-length image members were backed up outside the library and repaired by removing only those empty entries. All nonempty member payloads and metadata were retained and independently checked. A confirmed multi-volume work was consolidated into an ordered collection ZIP, retaining the original volume archives outside the library. Its first generated container was rejected by the native parser because prefixed member paths disagreed with inherited Unicode Path extra fields; those path-dependent fields and CRCs were rebuilt. The corrected container retained every original member payload and unrelated metadata and passed the actual native rescan. This is a file-packaging correction, not an application-code change or a page-completeness claim.

The final native directory rescan completed without file-review errors. A further pinned, add-only source registration was applied using the existing verified helper, with a fresh backup, whole-file hash verification, expected revision and exact delta audit. Existing source registrations, unrelated manga files, download history, following and saved author results were preserved. Bounded native checks passed for local availability, an added owned source record and the saved author-update counts; this is not a full-source crawl or a decode of every image in the library.

The known transient local-following busy indication cleared using the normal local-following refresh during acceptance; this does not claim a root-cause application fix.

The remaining source-identifier gaps are retained as optional evidence records for otherwise present local works, not an instruction for the user to re-identify every file. Neither local acceptance nor a missing source ID establishes a cross-source identity. Private counts, paths, titles, identifiers, backups and receipts remain under the private review directory's `project3-round3-20260919/user-decisions-20260919`. Previous transaction evidence is preserved. Do not replay the cleanup, rescan or import just to reproduce paperwork. No product code, build, installer, version, release, production setting or runtime automatic matching changed.

### Visual identity supplement imported, 2026-09-19

A bounded comparison of existing local cover/title pages with covers displayed by the native source-detail view resolved further explicit old-library identities. The independently pinned supplement was applied and verified with whole-file hashes, a fresh backup and exact registration-delta checks. Earlier transaction evidence was preserved, including its distinct native acceptance record. Existing registrations and unrelated application data remained unchanged; no manga files, application code or runtime matching behavior changed.

Post-import bounded native acceptance for this supplement passed separately: a newly registered source-detail sample showed owned after restarting the existing executable, and saved author-update counts remained valid. Private library, download, following and saved-check document hashes stayed unchanged. A transient local-following busy warning cleared with its normal refresh; its cause is not claimed fixed. This does not constitute an all-identifier scan. Private titles, identifiers, counts and source images remain outside Git. Do not repeat the applied import.

### Page-independent review continued and applied, 2026-09-19

The user clarified that some pages were deliberately removed and that page counts must not determine identity. The residual old-library review continued using explicit authorship, work/volume titles, copyright/translation attribution and existing source evidence. Page equality was not treated as identity proof; page differences and gaps no longer blocked otherwise confirmed identities. An additional pinned add-only manifest was imported with whole-file hashes, a fresh store backup and exact revision/delta checks. All previous registrations and unrelated store fields/documents were preserved. No manga bytes, downloads, application code, version, production setting or runtime matching behavior changed. The current private residual list distinguishes actual identity/volume ambiguity from empty files and unindexable zero-byte image members. Do not repeat the import; see private `project3-round3-20260919/page-independent-20260919` evidence.

Bounded native acceptance for this page-independent import passed on the existing executable: one newly registered item per source displayed owned and was excluded by the unowned filter; complete favorite caches and saved author results showed valid inventory counts without a new all-author scan or download. All four post-import private-store document hashes remained unchanged. Startup initially showed unknown inventory; normal navigation later recovered it without data or code changes. The cause of that transient was not established and is not claimed fixed. This is not an all-identifier native test.

### User-approved additional partial archives applied, 2026-09-19

The user explicitly accepted the separately enumerated remaining partial ZIPs under the same existing-file policy as the earlier accepted set. Only that exact list was applied. Existing known identities were rechecked; accepting missing pages did not invent identities for other chapters or language versions. The existing helper imported the pinned manifest after a fresh backup and revision check, verified whole-file hashes, and preserved all previous registrations and unrelated private-store documents. Original missing-page findings remain in private evidence; no manga bytes or download history changed. Empty and unindexable archives remain outside this authorization.

The same executable restarted successfully, an added owned-detail sample passed, and existing saved author results retained their owned/unowned filters without a fresh full-author scan. The current private aggregate and residual list were updated; complete transaction snapshots were retained. No code, build, installer, release, production change or runtime matching was introduced. Do not repeat this import. The private `project3-round3-20260919/accepted16-20260919` directory contains the applied manifest, exact audit, backup and report.

### Further private review applied, 2026-09-19; bounded native checks passed

The user resumed the residual review and approved further bounded metadata queries. Local archive structure, existing naming records, saved source metadata and in-memory text recognition were independently reviewed. A further explicit subset was imported with the same verified helper, pinned manifest digest and current private-store revision after closing the application and backing up its current documents. Whole-ZIP hashes and the exact post-write delta passed; existing registrations, other library fields and the user's intervening download were preserved. No manga bytes or download history were changed. Scope notes for complete works contained within a larger ZIP remain in private sidecars; do not claim that every unassigned chapter in such a ZIP was resolved. Empty/unindexable archives, unsupported language versions and other undecided file scopes remain excluded. The previously accepted partial-archive policy was not extended to new files.

The existing executable restarted successfully. Saved author-update counts and owned/unowned filtering remained correct without a new all-followed-author check. The private library, download, following and saved-check documents stayed byte-identical after import during the native checks. Two Pica detail reads returned metadata but insufficient language evidence, so those candidate relations were not promoted. Both bounded fresh JM single-work detail samples returned successfully and displayed the expected owned status. The initial attempts used a JM prefix accepted by the download entry but rejected locally by the source lookup parser; repeating them with supported numeric input passed. This corrects the acceptance procedure, not application or source behavior, and does not constitute a new full-author or all-identifier scan.

Private decisions, applied-manifest verification, a searchable residual list and the report are in the private review directory's `project3-round3-20260919` subdirectory. Some remaining identities are already known and need a file-treatment decision instead of another identifier search. The user separately approved the two additional exact JM detail reads. Both completed; a small supplemental manifest was then applied and verified with a fresh backup and revision, followed by a successful restart of the existing executable. No approved identifier query remains pending. A final coverage audit also read previously returned, authorized candidates and checked a small number of local attribution pages. A further bounded supplement was backed up, hash-verified, applied and reopened; an added native detail sample displayed its expected owned status. The prior import snapshots and unrelated private documents were preserved. The two bounded native JM source-detail samples subsequently passed using supported numeric input, as described above. Do not repeat the applied import or discard intervening user state. Runtime automatic matching remains absent. No code, version, installer, CI rerun, push, release or production change was made for this data/document-only continuation.

### Earlier follow-up review completed; unresolved identities awaited further review

The user asked the agent to continue the remaining old-library review and explicitly approved the prepared bounded title-fragment queries to the existing JM endpoint. That continuation is now applied using the same verified helper and executable. Existing source metadata, the authorized read-only queries, original naming records, ZIP structure and local text recognition were reviewed before importing an additional exact manifest. Whole-file hashes, current identities, the unchanged earlier registrations and the precise private-store delta passed verification. Confirmed same-work copies were retained without redundant bindings or deletion; unresolved identity, edition, chapter-range and empty/unindexable cases are held out. Private counts, titles and evidence remain outside Git. The searchable manual-review page and final report are in the private review directory's `project3-round2` subdirectory. No further source queries or manga changes are pending in this continuation.

The continuation's bounded native checks passed for the JM currently loaded first page, the complete Pica favorite cache and the existing saved author-update scope, including owned/unowned filters. Unlike the earlier pass recorded below, this JM visit reported its local cache temporarily unavailable and loaded only the first page; do not describe this continuation as a complete JM native replay. The full saved JM catalog was compared offline separately. The cache-read observation has not been root-caused; the original cache was retained. All four library/download/following/discovery document hashes stayed unchanged after the import during UI verification. The app is left on saved author updates filtered to unowned. Temporary extracted OCR images were cleaned up; reviewed manga ZIPs were untouched. Do not repeat the import, source scans, accepted checks or CI for this data/document-only continuation. The remaining uncertain items require user evidence or a separate scope decision.

### Earlier applied subset and verification

The user explicitly resumed and requested completion of the old-library review. They accepted the inspected 154 incomplete ZIPs as normal works; do not ask to repair those files before registering a confirmed identity. Private evidence and candidates remain outside Git. The new local-only review helper imports exact source references bound to verified existing ZIPs, separately from successful download records. The shared inventory used by favorites, search and author updates now recognizes those explicit registrations with current file presence. Runtime identity guessing and automatic association remain removed. See [scope, applied import and validation](REVIEWED_OLD_LIBRARY_2026-09-18.md).

Final head `6c8ea292af9eba6c13846f4b06993099df5bf8a2`, merge `1c3f29d8e51122f8c4d0f1c3c3540b70c9dceba1`, passed baseline `35343829531`, frontend `35343829577` and desktop `35343829532`. Verified helper and EXE are delivered at `Documents/Codex/MangaMonitor-Dev-20260918-6c8ea29`. The user expressly approved closing the old app, applying the reviewed list, starting this new executable, updating existing shortcuts and native acceptance. The exact-digest/revision import has completed with whole-ZIP hashes verified, and the applied references/file identities were independently checked. Original library fields, download history, following and saved author-check documents were preserved. Media were not modified; private candidates still needing identity decisions were not imported. Detailed counts and evidence stay in the private review directory outside Git.

The user completed JM login and the bounded native follow-up is now complete. Both JM/Pica favorites show the expected complete-range owned/unowned counts and filters. One fresh two-source author search completed returned pagination and recognized registered old ZIPs while retaining the separate other-keyword-result scope. The existing saved author-update view also recognized registrations; its owned/unowned filters passed without starting a new all-followed-author scan. All four private library/download/following/discovery document hashes remained unchanged during the follow-up. The page is left in the author-update unowned view. Desktop and Start Menu shortcuts point to the verified new EXE; backups and old executables remain. No new manga download, source write, release or production change occurred. Do not repeat the applied import or the passed acceptance solely to close paperwork. Remaining private identity ambiguities still require review; do not treat the applied subset as completion of all old-library identities or upload real manga titles, identifiers, files or private manifests.

## Current maintenance: final registration after a library rescan, 2026-09-18

The user completed a fresh whole-book image download after the Pica format repair and authorized removal of the old failed attempt. The new task has all 326 staged images with valid sizes/hashes, but failed before ZIP/index finalization with `LIBRARY_BUSY`: an identical-root rescan advanced the library generation. This is a separate desktop finalization defect, not another decode failure. The prior task's 57 staged images and queue record have now been removed under the user's explicit deletion scope, with exact-path/hash checks and the app closed; the new staged media, other three records and library remained unchanged. Private cleanup evidence and the old queue JSON backup stay outside Git.

The scoped code repair admits the current completed index generation only on an explicit retry/continue of the identical native root. Original approval/staging/output bindings remain immutable; the active process pins the newly admitted generation and receipts use it for registration. Changed roots, stale controls, unfinished/newer rescans and forged receipts remain blocked. See [the repair and regression scope](DOWNLOAD_RESCAN_FINALIZATION_FIX_2026-09-18.md).

Final head `9a3b566721246765306afebc8fb6fcfa2b871088`, merge `03feac933d03fa1ffa86c601e23be4a41e3bd436`, passed baseline CI `35254100302`, frontend `35254101814` (149 logic / 116 Chromium), and desktop `35254100355` (Windows download module 65 tests, other modules/credentials, 34 native IPC, Clippy, EXE and actual WebView startup/restart). Initial CI found two malformed synthetic fixtures; they were corrected without weakening product checks. Dev artifact `10511239309` was digest/CRC/PE/revision checked and delivered to `Documents/Codex/MangaMonitor-Dev-20260918-9a3b566`; EXE SHA-256 `c9578d3d2619aea703f31449f7b228855e45e66e12aadb23d82ba7fbddcf3403`. Version stays 0.3.4, no installer, old EXE retained.

On the exact new executable the agent clicked Retry once on the already staged task. It completed ZIP saving and native registration; the linked PC detail visibly reports owned, ZIP and 326 pages. ZIP CRC, aggregate/per-file hashes, actual image suffixes and all 326 original staged image hashes passed. Original task bindings/report/checkpoint and the other queue records remain unchanged. Normal successful registration removed this task's staging; the explicitly removed old failure remains absent. The separate historical missing-file record was not removed or retried. Native repair verification is complete; it is not a new user-acceptance statement. Next step is discussion of post-V1 ideas, with no automatic new batch, re-download, release or production enablement.

## Previous maintenance: Pica image-format repair, 2026-09-18

V1 final product experience remains accepted. The user reported a failed Pica download and explicitly requested its repair before post-V1 discussion. A bounded read-only diagnostic confirmed a valid WebP body behind a JPG URL and JPEG MIME label, matching the next chapter after the preserved progress. The repair detects supported Pica content formats, retains original bytes and descriptor hashes, derives the actual output suffix, and preserves resume/manifest/ZIP consistency; JM stays strict. See [the scoped repair](PICA_MEDIA_FORMAT_FIX_2026-09-18.md).

Final head `037f770ac9fd5eae60fd75b2a37e9759bbfb7dfd` passed baseline CI `35247966981`, frontend `35247966956` (149 logic / 115 Chromium) and desktop `35247966953` (modules/credentials, 34 native IPC, Clippy, executable and actual CI WebView startup/restart). Initial functional regressions passed; a Clippy enum-size warning was corrected with a boxed verified pipeline value before final CI. Dev artifact `10508174894`, test merge `d11482871e21c21eaa9f4d709f03b81480d6ad56`, is delivered separately at `Documents/Codex/MangaMonitor-Dev-20260918-037f770`. EXE SHA-256: `30b922848242e81461f36b37a5b38886e1f906b51ab0e2ccc74dfcd7f1bfc81e`. GitHub archive digest, CRC, x64 PE, embedded revision and post-launch executable hash were checked. Version remains 0.3.4, no installer, old executables retained, draft/unmerged PR #19 and production disabled.

The exact new EXE starts locally and restores the existing queue/progress. During its initial bounded check download history remained byte-identical and no download or library mutation occurred. The old failed task was bound to a previous temporary root, so the user then prepared the same work normally in the current library; its complete image stage succeeded. Final ZIP/index registration exposed the distinct rescan defect recorded above. Do not edit private task bindings to work around it. Prior V1 acceptance is not reopened. The five preexisting acceptance edits were preserved, backed up and included in the image-format repair commit; its final evidence-only edits are carried with this next necessary repair.

## Accepted V1 baseline

On 2026-09-17 the user explicitly confirmed: “v1最终体验验收通过”. This closes V1 product-experience acceptance for code `49c40e843009ca968b595e0fa5009fc096b949cd` and the separately delivered 0.3.4 Dev executable. Do not ask the user to repeat final acceptance. The delivery remains that executable; no version bump, installer, merge, release or production-setting change was performed for this status update. Next work is maintenance or separately chosen post-V1 scope, not an automatic new development batch.

The preceding repair addressed the report that the Pica source-search page still returned the previously identified unrelated work. `44da160` fixed the independent author-search/update panels, but `SourceWorkbench` still displayed generic keyword hits in source search and the followed-author “search this author” entry. This is an application coverage gap. The current repair shares the same author-field predicate across these entries, defaults source search to an explicit author mode, preserves an explicit work-keyword mode and the single-ID/link path, and separates other keyword hits from author counts and selection. See [the current repair](SOURCE_AUTHOR_ENTRY_FIX_2026-09-17.md) and [the V1 readiness report](V1_READINESS_2026-09-17.md).

Current code `49c40e843009ca968b595e0fa5009fc096b949cd` has passed all three CI workflows. The separate Dev executable was verified and delivered, then launched on 2026-09-17 after the user resumed Computer Use. Frontend `35236666931`: 149 logic / 115 Chromium, format, types/build. Baseline `35236666896` passed. Desktop `35236666854`: module/credential checks, 34 native IPC, Clippy, EXE and CI WebView startup/restart passed. The first run exposed an unconfigured-library synthetic fixture in two new count assertions; the fixture was corrected without weakening assertions or product ownership rules. Four new author-entry scenarios now pass. The two synthetic screenshots were reviewed. The fresh Pica source-author/keyword checks and the JM followed-author entry have now passed within the bounded samples below. The user has now separately accepted the final V1 product experience; this is not a claim that a public release was performed.

Artifact `10504511036`, test merge `e723b3a97180d471e600c96f95768b3d562cebcd`; ZIP SHA-256 `0e44feb2f1f4144a01cec28954e536c9c5a069797720b411b178e2f732bd9c73`, EXE SHA-256 `6353f05ad2ac3a6b49c8a76af245a30dba27dabb2d53d1f31fcdcfc86c274176`. Delivery: `Documents/Codex/MangaMonitor-Dev-20260917-49c40e8` (0.3.4, no installer). CRC, GitHub digest, PE and embedded head verified. The old executable was preserved. A narrow cached-real-metadata replay previously confirmed identical author partitions across entries, exclusion of the mistaken sample and unchanged private store bytes; that replay made no source requests. During native follow-up the registered old demo was briefly opened by the launcher, recognized and closed without product actions; the exact delivered executable was then launched by its process path and its hash rechecked.

The user resumed Computer Use for the preceding bounded native checks. Native follow-up on the exact delivered executable completed a fresh Pica source-author query: 12 raw records, 6 author records and 6 other keyword hits. The default list excludes the mistaken sample; select-all selects only 6, changing to other hits clears selection and removes batch controls. The mistaken sample is visibly unowned. Explicit keyword mode returns all 12 with its scope warning. Pica local following initially showed a busy message, then one spaced manual retry succeeded with an empty list. The user completed JM login. The existing followed-author search then read all 15 returned records: 15 explicit author records, 0 other hits, with all 15 source author labels checked. Select-all selected 15; switching to other results cleared selection and removed batch controls, and returning left selection cleared. The accepted English-category scope was visible. No downloads were prepared or started and no follows were changed. Library, download-history, saved author-check and following document hashes remained unchanged across this JM pass. Evidence is in the private `native-49c40e8/jm-followup` directory.

Both source-follow lists initially showed a busy message and recovered after one spaced manual retry. The specific cause was not captured, so this pass does not claim to have fixed that recoverable read issue. The author-entry regression itself passed after recovery. The user subsequently accepted V1 after this observation was reported. Keep it as a known recoverable maintenance observation rather than claiming a root-cause repair. Do not repeat accepted suites, media downloads or large author audits solely to close paperwork.

## File removal verification

The user expressly requested deletion of the earlier mistaken download and that it no longer count as owned. The exact single ZIP was checked by path, source ID in metadata, byte size and SHA-256, then deleted. The private deletion receipt is outside Git under `Documents/Codex/MangaMonitor-v1-readiness-20260917`.

The normal native library refresh has now completed. A bounded read-only check confirms that the deleted file remains absent and its old indexed record is gone; the online other-results view visibly reports it unowned. The live private store was not directly edited. Download-history and saved author-update document hashes are unchanged across this acceptance pass; only the normal library refresh updated the index. No new media was downloaded or deleted. Private evidence, hashes and the pre-update report backup are outside Git under `Documents/Codex/MangaMonitor-v1-readiness-20260917/native-49c40e8`. Receipt-based ownership still requires the actual file; history alone is insufficient.

## Completed acceptance to reuse

- Settings keyword interaction passed on the existing candidate on 2026-09-17: the user typed “记住会话”; only the account category remained, and clicking clear restored all five categories. The agent performed the observation/clear check before Computer Use was stopped. This does not need another replay solely because the native helper cannot type.
- The [2026-09-09 acceptance](LOCAL_WORKBENCH_COVER_SESSION_2026-09-09.md#验收状态与下一步) explicitly includes a user-selected website-favorite write plus website refresh. Reuse that bounded result; it is not an exhaustive two-source/add-remove matrix. Do not require arbitrary new favorite mutations.
- On `44da160`, saved/fresh author-update checks showed 21 explicit author records and 6 other keyword results. Actual select-all selected 21, then was cleared without preparing downloads. Independent two-source search returned 140 raw records: 69 explicit author records, 71 other hits, one same-source owned receipt. Owned filtering and queue/search navigation passed. Not all 71 are asserted unrelated; incomplete aliases/metadata remain inspectable.
- Earlier delegated native checks cover sessions, Explorer item selection, diagnostics/version/category navigation, one successful ZIP/receipt update after one explicit retry, and normal restart persistence. The wrong-author sample proves only download/receipt behavior. Its first `DOWNLOAD_FAILED` cause was not captured and must not be called fixed.
- Complete-author all-owned messaging and same-new-sample favorites/rank synchronization have synthetic integration coverage; no whole-author real download was manufactured. The prior hundred-author audit covered paging/retention, not complete bibliographies. Do not repeat it without new cause.

## Verified baseline and working copies

- Canonical checkout: `Documents/ChatGPT/多agent协同/MangaMonitor-AI`; writable worktree: `Documents/Codex/MangaMonitor-pc-zip-B-20260913`. Both code revisions are synchronized to `86f7b4cd9ac2690f0c8834dc80d183e928aa719d`; the accepted V1 baseline remains `49c40e8`. Final evidence-only local reports accompany the next necessary push. Existing branch: `codex/local-workbench-preview`, draft/unmerged PR #19; remotely rechecked 2026-09-22.
- Current exact-head CI: frontend `35691141971` (169 logic / 149 Chromium), baseline `35691141968`, desktop `35691141989` (86 account / 70 source / 38 native IPC, module/credential checks, Clippy, EXE and Windows WebView startup/restart), all passed. These workflow conclusions were freshly rechecked; native evidence boundaries are recorded above.
- Current tested merge `07ff073fa85b768c9d564dad6cbbbfb83ca5d718`, version 0.3.4; EXE SHA-256 `4771577ecf66dbbcbd19335fd2b959537050b28a49e6430b0db0c4fe86c22488`. Delivery: `Documents/Codex/MangaMonitor-Dev-20260922-86f7b4c`. Retain existing rollback deliveries and private receipts. No new installer was created.

## Current product rules

Use [the accepted V1 scope](V1_SCOPE_MANUAL_UPDATES_2026-09-14.md), with the separately authorized post-V1 old-library import and incremental checks above. Ownership requires a successful same-source/work download receipt or an explicitly reviewed old-library registration, plus its current verified file. Author checks are user-triggered while the app is open; no startup/closed-app/background monitoring or automatic download. First/full scans and ad-hoc searches retain complete raw pagination. Incremental checks explicitly distinguish retained historical scope from the pages checked now. JM excludes English Manga. The all-owned author claim requires a complete, nonempty unfiltered author scope and no unresolved keyword results; incremental completion does not renew that claim. Queue/selection capacity remains 500.

Do not restore runtime identity matching, interactive manual/cross-source association, dislike, language/version guesses, translation replacement, phone lists or classification booklists. Old-library assistance is now explicitly resumed within the scope above; reader and in-app updater remain separate post-V1 discussions. Final V1 product experience is accepted. PR remains draft/unmerged and `production_enabled=false`; no release operation is included.

## Preservation and verification

Formal suites/builds stay CI-only; local editing, formatting, narrow offline diagnostics and artifact inspection are distinct work. Prior user authorization covers pushes/CI/PR updates to the existing branch. No real titles, IDs, credentials or private library records enter Git/PR.

The preexisting six report edits were backed up before consolidation with hashes under `Documents/Codex/MangaMonitor-v1-readiness-20260917/preexisting-reports`. Preserve all deliveries, backups and stashes `13567c6224036f4605b02ebc9d41fd2fcc7b9f01`, `29e11022afb9b1ed21c8febabc61e9c643a179e6`, `7396192e02ae4fd4f90e46e7de6859431540cb25`. Never reset/clean them. Before the canonical checkout was fast-forwarded, the exact six original modified reports were also preserved in stash `293da6ca5702e29942fd5f4207d45b29daf9f872`. Final local evidence-only report updates are mirrored to both checkouts; carry them with the next necessary code push, without a documentation-only rebuild.

The two preexisting image-format final reports were additionally backed up under `Documents/Codex/MangaMonitor-rescan-finalization-20260918/canonical-preexisting-reports` and preserved in stash `547af6662727af39f6d8a7f5facb93936f7eb1ef` before this repair's fast-forward. Keep that stash and the private cleanup/ZIP verification evidence. Final local report changes are intentional and do not alter the CI-tested executable.

Before the project-3 canonical fast-forward, the three preexisting maintenance-report edits were additionally backed up under `Documents/Codex/MangaMonitor-old-library-review-20260918/project3/canonical-preexisting-reports` and preserved in stash `38ba773dce41f6c39677fff10be1695345fdf0c9`. Their final evidence is included in the new head. Retain this stash as well as earlier backups. The final project-3 evidence-only report edits are mirrored locally and should travel with the next necessary code push, without a documentation-only rebuild.

Before the incremental-check canonical fast-forward, the two preexisting project-3 and followed-author reports were verified against their private backup and preserved in stash `5df3752a0805ab4222cce31eaa5412210f9d41ab`. Their evidence is included in the tested code head. Keep this stash and all prior stashes; the two current final acceptance edits are intentionally local.
