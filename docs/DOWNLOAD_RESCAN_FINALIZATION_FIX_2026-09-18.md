# 下载完成后入库被目录重读阻断的修复

## 已确认的问题

用户重新下载后，图片已全部取得，但任务停在 `LIBRARY_BUSY`，没有生成最终 ZIP，也没有登记漫画库。这与前一项 Pica 图片格式兼容问题不同：完整暂存清单及图片哈希有效，失败发生在保存／入库阶段。

目录每次重新读取都会递增索引代次。旧下载任务把该代次与来源、目标目录、授权和暂存凭据绑定；原实现即使目录读取已结束，仍要求重试时与最初代次完全相同，因此同一个目录的正常重读也会使任务无法继续。不能通过直接修改私人任务的绑定来恢复。

## 修复范围和边界

- 用户明确点击重试／继续时，校验当前目录 ID、路径与实际文件身份均与原任务一致，且新的目录读取已完成，才为本次运行记录当前索引代次。
- 原下载任务的代次、授权、目标哈希、来源编号和暂存／输出凭据不变。当前索引代次只保留在本次进程的运行许可中，供最终登记使用；重新打开程序不会自动运行任务。
- 运行中再次重读目录仍会阻止旧运行继续写入。更换目录、目录实体被替换、读取暂停／未完成、过期控制和伪造登记凭据仍拒绝。
- 已有完整暂存清单时，校验并复用图片完成 ZIP 与入库；ZIP 已完成但登记失败时，校验并复用原 ZIP，不覆盖其他文件。
- 队列明确提示“图片已下载齐，保存或入库尚未完成”，并把 `LIBRARY_BUSY` 提示改为完成目录读取后重试当前操作。

已按 `DOWNLOAD_EXECUTOR_THAW_GATE.md` 复核桌面保存、登记、重试及其凭据契约。本次不更改来源协议、图片处理、并发或请求策略，复用现有固定上游审查。生产关闭、来源所有权规则、版本与发布状态保持原约束。

## 验证计划和真实操作

新增离线回归覆盖同目录重读后的目录／ZIP 保存、保存后登记前重读、再次重读与重启使运行许可失效、批量继续、不同目录与文件身份拒绝，以及 Pica 混合图片格式完整进度的 ZIP 入库。新增前端场景验证完整图片进度仍属于“需要处理”，重试登记后才变成“已下载”。正式测试和构建仅在 CI 进行，本机不重复运行。

首轮 CI 已通过主要收尾回归，同时指出两处新增测试构造问题：错误状态的合成目录缺少必需错误字段；前端合成完成事件未等待重试控制响应。已补齐错误状态并断言具体拒绝码，前端先断言重试状态再发送完成事件，不放宽产品检查或等待上限。最终结果以修正后的提交验证为准。

按用户明确要求，旧失败任务已在程序关闭、锁定私人存储后定点移除，其 57 个旧暂存图片也已删除。删除前核对精确路径、文件身份、全部文件大小和哈希；其他 3 条记录、新任务 326 个暂存图片及漫画库索引均保持不变。详细回执、队列 JSON 备份与真实编号只保存在 Git 外，没有删除漫画库作品。

## 最终验证和交付

最终提交 `9a3b566721246765306afebc8fb6fcfa2b871088`，测试合并 `03feac933d03fa1ffa86c601e23be4a41e3bd436`：

- [基础检查](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/35254100302)通过，包括工作区测试、格式／Clippy、Windows 核心检查；Linux 下载模块 67 项通过。
- [前端检查](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/35254101814)通过：149 项逻辑测试、116 项 Chromium 场景、格式、类型与构建。
- [桌面检查](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/35254100355)通过：Windows 下载模块 65 项、其他模块／凭据检查、34 项原生 IPC、Clippy、EXE 构建和实际 WebView 启动／重启。

[工件 10511239309](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/35254100355/artifacts/10511239309) ZIP SHA-256：`23650df203cd10b15e7a5df664300b4a68213f25e160ada602b13754578fb2cc`；EXE SHA-256：`c9578d3d2619aea703f31449f7b228855e45e66e12aadb23d82ba7fbddcf3403`。已核对 GitHub 摘要、CRC、x64 PE、内嵌完整提交号及启动后的 EXE 哈希。单独交付于 `Documents/Codex/MangaMonitor-Dev-20260918-9a3b566`；版本仍为 0.3.4，不生成安装包，不覆盖旧 EXE。

在确切修正版的原生队列中观察到新提示及保留的完整进度，点击该任务一次“重试”后成功保存 ZIP 并完成正常入库。随后从该任务“查看电脑文件”进入漫画库详情，实际显示“已入库 · 电脑漫画库”、326 页及 ZIP 文件信息。当前目录已经读完，不需要另行全库重读。

最终只读文件核验：ZIP CRC、整个 ZIP 及各清单文件 SHA-256 均一致；326 张图片的字节数和哈希与重试前的完整暂存逐一对应，其中 59 张 JPEG、267 张 WebP，扩展名与格式一致。原下载绑定、暂存清单和其余任务记录未变；已完成暂存由应用在成功登记后按正常流程清理。旧失败任务／旧暂存继续保持不存在。队列仍有另一条先前已删除作品的“文件已移除”历史记录，本次没有操作它。

原生页面已对照队列基线及后续已接受的 V1 行为，保留作品行、状态操作与导航；新增提示可见且完成后消失。代理的本次实机验证通过，不将其写成用户的新一轮验收。V1 既有体验验收仍成立。PR #19 继续 draft／未合并，`production_enabled=false`，没有发布操作。后续想法现在可进入讨论；不自动恢复扩展开发。

真实路径、作品编号、文件哈希与清理回执留在 Git 外的 `Documents/Codex/MangaMonitor-rescan-finalization-20260918` 及之前的私人诊断目录。主检出原有两份报告已备份，并保留 stash `547af6662727af39f6d8a7f5facb93936f7eb1ef`；两个检出代码均为最终提交。最终证据文档留待下一次必要代码推送携带，不为文档重复构建。
