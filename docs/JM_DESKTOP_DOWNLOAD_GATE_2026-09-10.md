# JM 桌面单本下载批次契约与本轮审计

## 2026-09-12 上游并发结构复核（当前）

手动单本元数据不再误用监控每请求 1–3 秒的随机等待：只对已授权桌面下载中的 `/album`、`/chapter` 采用上游即时节奏。默认客户端、其他 API 和云监控节奏保留；沿用原 preflight/媒体枚举授权和完整证据校验，不更改域名/重试范围。

用户报告两路版本仍慢，并取消本次下载的低电脑负荷限制。重新打开三份固定上游及当前契约/测试后，图片生命周期窗口提高到 20，GET 使用独立异步任务，解码/还原/JPG编码/输出验证与哈希在 CPU 线程执行。已派发 CPU 工作实际退出后才释放下载 worker。私有记录复用的前提是重新安全读取全部字节且完全相同，内容变化重新验证；当前批准、任务/会话/目录代次、按页持久化和完整登记保持。未增加真实来源访问范围，未启用 Pica 桌面下载、跨作品调度、覆盖或生产。具体实现和验证分配见 [当前报告](JM_UPSTREAM_PERFORMANCE_2026-09-12.md)。下节两路并发为此前版本记录。

## 2026-09-12 JPG / 有限并发复核

上一批功能已获用户真实验收。用户指出输出格式和速度差异，本轮再次读取本契约、thaw gate、upstream reference 和三份固定上游实际源码，记录于 [修复报告](JM_JPEG_PERFORMANCE_2026-09-12.md)。新增的是批准绑定的静态 JPG 输出和两路媒体 future 窗口，复用相同图片还原规则及有序暂存证明。旧 WEBP 任务沿用原绑定；GIF 原字节。窗口丢弃即取消未完成请求，写入/检查点串行，批准、会话、目录代次持续复核。本轮不扩展下载作品范围，不授予覆盖/替换/生产权限，不转换既有作品，也不启用 Pica 桌面下载。

## 范围和当前位置

双库批次已经完成代码和 CI。本批实现用户在桌面程序主动选择一本 JM、确认目录和作品后下载，完整保存并登记电脑库。手机名单保持独立，传入手机后不删除电脑文件。Pica 下载、批量调度、自动更新和安装包不在本批。

用户已要求开始下一批，并指定一个私有真实验收样本。开发、合成验证先行；真实运行须在代码与 CI 检查后显示具体作品、目录和操作范围，不能由测试代码自动启动。真实编号不写入公开测试或文档。

## 本轮重新打开并对照的上游

2026-09-10 通过 GitHub 读取以下固定提交的实际文件，未改变协议 pins：

- `lanyeeee/jmcomic-downloader` `f0cdd724af6892002f2fb7be883b88832cebe7e9`：`src-tauri/src/downloader/download_task.rs`、`download_img_task.rs`、`src-tauri/src/jm_client.rs`。
- `lanyeeee/picacomic-downloader` `77c8b62ede42b3afc074506d092313816af8092d`：`src-tauri/src/download_manager.rs`、`src-tauri/src/pica_client.rs`。
- `hect0x7/JMComic-Crawler-Python` `9fddb0494caf0cdc812ac6cbfc1c62f4f845b058`：`src/jmcomic/jm_downloader.py`。

同时阅读 `DOWNLOAD_EXECUTOR_THAW_GATE.md`、`upstream-source-reference.md`、`V1_ADD_ONLY_DOWNLOAD_THAW_2026-09-08.md` 和目录兼容记录。上游代码只提供协议、图片处理与完成条件证据，不提供本项目文件删除或生产授权。

JM 使用固定 API 域、按当前代次重新核验的章节/图片枚举和既有图片还原；媒体客户端不携带账号凭据。保留 GIF，WEBP 采用已实现的固定分块还原，不照抄上游缺少 scramble 时的猜测默认值。所有图片和章节都成功才形成完整证明。Pica 和 Python 同样要求分页、图片、章节整体完整，没有因为部分成功就结束的语义。

上游存在删除既有目标后重命名、清理临时目录、仅凭文件存在跳过、元数据保存失败仍报告成功等行为。本批不采用这些行为。现有固定域有记录的请求逻辑保持，不增加隐藏重试或无限并发。

## 桌面手动任务与已有核心的关系

桌面私有任务明确记录 `origin=manual`，由用户确认所见作品和最终目录产生授权，绑定 task/revision/target/root identity/layout。只投影实际当前任务和批准到 A6 `State.pending` / `GateLedger`，复用 `execute_live` 的当前代次、预检、媒体描述、隔离暂存、哈希清单和收据验证。

这不是云端作者范围的 `PROVEN_NEW` 证据。不得合成 matcher 结论或调用 V1.5–V1.8 的生产库存/任务完成链来伪造符合该条件。既有入口和 production-disabled 状态保持。实际网络执行在 GitHub Actions 中拒绝；合成测试只使用注入的测试传输和临时目录。

本批新增的是明确的桌面手动新增目录物化层：批准绑定选择器目录身份、最终新目录名及布局版本；暂存完整后再绑定 exact manifest 与布局映射；立即重新检查任务/会话/目录身份，然后只向本任务拥有的新目录写入。不得覆盖既有作品或替换目录。元数据、封面和图片布局构成独立的最终清单；整个树复核后才可交给电脑库登记。新目录先写入持久布局标记，普通扫描在最终清单齐备前不发布该目录的已下载记录。暂存完成不能称为已下载。

## 交互与持久化契约

- `jm_download_prepare({scope:{source:'JM',sessionId},input,rootId,generation})`：只预备计划；编号/受支持链接解析在 native，renderer 不传物理路径、媒体 URL 或凭据。
- `jm_download_confirm({planId,expectedRevision})`：确认仍然当前的计划，持久化并启动一本任务。
- `jm_download_read({})`：只读恢复。应用重开后未完成任务暂停，不能自动开始联网或写漫画文件。
- `jm_download_control({scope,taskId,expectedRevision,action})`：显式暂停、继续或重试。控制 revision 与图片进度分开，旧代次不能再次写入。
- 单本单 worker。暂停在当前在途请求结束后的下一授权点生效；保留可验证的已有结果。重试不能将残缺作品误报完整，也不能因登记失败重新下载完整漫画。

任务响应仅含作品、显示目录、状态、数量与安全错误码。计划和运行账号会话只存内存，无密码/token 入队列。读坏私有文档不能回退成空队列后覆盖原文档。

输出采用既有下载器目录约定：作品目录、`元数据.json`、封面、章节目录、`章节元数据.json`、四位页码图像。已完整物化后只扫描这一目录，匹配来源编号和页数，电脑索引保存成功才报告 `downloaded`。不改变手机名单、不全库扫描、不删本地副本。

## 验收与分工

核心负责私有任务、A6 适配、低并发执行、暂停/续接、最终目录与证明；native 负责主窗口 IPC、账号会话、选择器目录代次和定向登记；UI 负责具体确认、真实队列和移除 native 模拟进度；独立检查负责定向登记和边界回归。

正式编译与测试只在 CI。覆盖：取消无任务；失效计划/会话/目录拒绝；暂停/重开不自动执行；失败不完成；已验证文件复用；既有目标不覆盖；部分/错误 ID/错误页数不能登记；手机状态不改；真实任务没有模拟计时器。CI 通过与真实验收分开记录，未完成项目不能标成通过。

## 本批临时数据生命周期补充（2026-09-11）

用户要求保留电脑作品，并关注不必要的本机存储负担。桌面下载在最终作品完整复核、PC索引成功登记且任务完成持久化之后，可释放本任务自行创建的私有暂存副本。清理只针对已验证manifest中的对应临时文件及已空目录；未知文件、身份变化或路径重定向均保留。暂停、失败的任务保留续接数据。此能力不接收renderer路径，不触及最终作品、原有漫画、手机库或其他任务，也不改变旧A6收据的production/replacement/physical-delete flags。它是用户在桌面确认的单本下载中程序临时数据的生命周期，不能被复用于生产库删除。此处仅记录开发范围，真实样本仍待CI后的具体目录与执行确认。
