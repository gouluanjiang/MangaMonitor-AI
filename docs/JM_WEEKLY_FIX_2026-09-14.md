# JM 周榜修复与搜索范围说明

2026-09-14，接续已交付的 `6f794fd` Dev 版本，处理用户本轮验收反馈。最新修订 `6325982` 包含已完成的周榜修复与用户确认的 JM 搜索范围说明，全部必需工程检查已通过；以下保留独立验证及先前 `524f256` 的历史证据。

## 最新交付：6325982

交付目录为 Documents/Codex 下的 `MangaMonitor-Dev-20260914-6325982`，其中 `MangaMonitor Dev.exe` 的版本仍是 0.3.4，大小 20,234,240 字节。退出旧 Dev 后打开这一份，无需重新安装或重新导入漫画库。旧程序文件没有被覆盖，新程序未自动启动。

| 检查 | 结果 |
| --- | --- |
| [前端 CI 34828965934](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34828965934) | 139 项逻辑测试、96 项 Chromium 界面测试全部通过；格式、类型与构建通过。 |
| [基础 CI 34828965947](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34828965947) | Rust 工作区与 Windows 核心检查全部通过。 |
| [Windows CI 34828965929](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34828965929) | 周榜空标题回归、模块／原生接口、Clippy、Dev 构建与实际 WebView 启动／重启检查全部通过，安装包步骤保持跳过。 |
| 页面检查 | 已查看同修订的作者搜索、作者更新与 JM 周榜截图；范围说明与期数／类型显示正常。使用合成数据，不替代用户真实使用验收。 |

对应代码 `6325982c924286fac19742e1f1a60f0a6e66f578`，PR 测试合并提交 `6d494b3ea7edb9049f2ca297dfb81f4d9c3a15e1`。已核对 [Dev 工件 10341925776](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34828965929/artifacts/10341925776)：

- ZIP SHA-256：`bac02d21aea67acfbaebccd1540e9099a2fa8b183b7fedd6f33a3cd578a7c1b5`，与 GitHub 摘要一致且 ZIP CRC 通过。
- EXE SHA-256：`11f6c7e75979fd2598a1a2f529b2b248c161745a315b48511a8a35a409c8db76`。

待用户复测两处：JM 周榜可读并能切换期数／类型；JM 搜索和作者页面能看到已确认的 English Manga 范围说明。未要求重新导入或核对旧漫画库。真实来源诊断、工程验证与用户验收继续分别记录。

主项目与现有开发分支已同步到这一修订，PR #19 保持草稿、未合并。此前未提交报告先备份并保留到定向 stash；本节最终证据留在本地，随下次必要代码推送纳入，不为文档重复构建。

## JM 搜索数量差异与已确认范围

用户授权在其已打开的 Chrome 搜索页复测。两组公开搜索样例分别为：网页 145 条、其中 English Manga 分类 54 条，软件来源查询返回 91 条（80 + 11）；网页 112 条、其中英文分类 4 条，软件来源查询返回 108 条（80 + 28）。两组差额与英文分类数量一致。网页使用普通搜索框，来源查询没有对结果另做作者名过滤；补充全时间参数也未改变结果。

因此，本次没有发现软件漏读分页；差异属于 JM 网页与当前搜索接口的覆盖范围。用户已明确选择 V1 **暂不纳入网页端 English Manga 分类**，不新增网页抓取或扩大来源请求。Pica 搜索范围保持原样，本次没有进行 Pica 搜索数量对比。

JM 来源搜索、双来源作者搜索及作者更新页新增范围说明；单独筛选 Pica 时不显示 JM 说明。搜索完成文案表述为读完当前来源的搜索范围，完整统计仍以所有分页读取完毕为前提。收藏自身的范围文案与读取方式不变。

这一轮另进行了六次匿名公开搜索文字请求，核对两组首页、全时间参数及尾页；浏览器检查完成后恢复用户原来的搜索页面。没有修改账号、收藏、私人库或下载漫画文件。公开对照及请求细节保存在本机诊断记录中，不把用户的完整查询内容或浏览器记录加入项目。

## 已定位的问题

JM 周榜打不开是项目的解析问题。读取公开的 `/week` 返回了 256 个期数，其中 174 个 `title` 为空，包括当前期数；日期与期数文字保存在 `time`。原代码要求所有标题非空，因而一条空标题就阻止整个期数列表读取。

已固定版本的 JM 下载器 `WeeklyPane.vue` 本来就用 `category.time` 显示期数。采用这一字段不需要修改接口、账号、域名或漫画解析规则。

另一个体验问题是默认选择了第一个类型“韓漫”。诊断时它确实返回 0 条，来源另一个“日漫”类型则返回 20 条且总数为 20。空类型不是网络错误。

## 修复内容

- 期数标题可为空，使用日期文字；日期和标题都为空时显示明确的期数 ID，不丢掉对应榜单。标识、重复项、数据类型和文本长度继续校验。
- JM 默认选择来源提供的“日漫”类型；仍可切换所有原有类型和期数，Pica 默认周榜不变。
- 首次失败不再误称“已读取榜单保留”；有当前结果时仍保留它们。正常空榜单提示切换期数或类型。
- 收藏、搜索、作者更新和榜单统计旁统一说明：按本软件对应来源的下载记录和实际文件统计；未登记的旧漫画可能显示“未入库”。不改变状态判断。

## 旧漫画的后续安排

用户已再次确认不恢复自动匹配。旧漫画即使存在于磁盘，缺少相应来源的有效下载登记时仍不计入线上“已入库”。用户反馈的 0 部与这个口径一致，不代表磁盘上不存在该作者作品。本轮未检查用户的具体文件，也不声称完成了该作者的私人库核对。

现有旧漫画的人机辅助核对改为 **V1 完成后处理**，届时确定核对方式及独立登记依据。当前不实施、不把旧文件伪造成软件下载记录，不恢复自动或跨站匹配。

## 验证边界

- 真实来源诊断：2026-09-14 16:59–17:01（本机时间），总共四次匿名公开文字 GET；两次期数列表、两个类型各一次。返回结构确认了空标题原因及非空榜单字段。没有账号访问、图片下载、私人库扫描或数据修改。
- 工程回归：添加空期数标题、日期及 ID 回退的来源测试；非法类型、超长值仍被拒绝。现有界面回归增加日漫默认值、日期标签、正常空榜单及首次失败不误报保留的检查。
- 正式检查与 Dev 构建只在 CI 运行，本地只编辑、格式化、审查及进行上述独立诊断。修订 `524f25673ad465210a72a567c7ef9cdbe33a0e2f` 全部必需 CI 已通过，证据如下。
- 本轮不改变下载器、文件登记、生产开关、软件版本号或安装方式；修复后的应用真实使用验收与公开接口诊断分别记录。

后续继续 [当前 V1 路线](V1_SCOPE_MANUAL_UPDATES_2026-09-14.md) 的完整范围批量选书与界面收尾。

## 周榜修复 524f256 的工程结果与交付

| 检查 | 结果 |
| --- | --- |
| [前端 CI 34826250893](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34826250893) | 139 项逻辑测试、96 项 Chromium 界面测试全部通过，包含本次改动的三个榜单场景；格式、类型和构建通过。 |
| [基础 CI 34826250889](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34826250889) | Rust 工作区、Windows 核心检查及现有保护规则全部通过。 |
| [Windows CI 34826250960](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34826250960) | 两个新增空标题／非法值回归实际执行通过；模块、原生接口、Clippy、Dev 构建和实际 WebView 启动／重启检查全部通过。 |
| 界面审查 | 已检查同修订的 JM 周榜、作者搜索、在线收藏截图，日期、类型和统计说明无重叠；图片为合成数据，不代表真实封面验收。 |
| 真实使用 | 公开接口原因已定位；修复后的应用仍待用户复测 JM 周榜。 |

交付 `MangaMonitor-Dev-20260914-524f256/MangaMonitor Dev.exe`，版本仍为 0.3.4，大小 20,234,240 字节。先退出旧 Dev，再打开这个修复版，进入“发现 → JM 每周必看”，检查默认日漫榜单和期数／类型切换即可；无需重新安装或重做漫画库导入。

- [已核对的 Dev 工件](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34826250960/artifacts/10340790412)，对应 PR 测试合并提交 `57e1a3e5d65eb39b196d2d6683edc9e58cbcedb8`。
- ZIP SHA-256：`0106d6483f2fca0eeef7e2e3ba2a70d20a0c7c764493f7775c5f38b5967231c1`，与 GitHub 摘要相同且 ZIP CRC 通过。
- EXE SHA-256：`621db39933e28f0757fbd0eb8e4daea031808cf49b9cbf8ea0c592b63b36a1d1`。

代码已推送现有开发分支，PR #19 保持草稿，主项目已同步，原有未提交报告经备份保留。最终证据补充留在本地主项目，随下次必要代码推送提交，不为文档单独重复构建。
