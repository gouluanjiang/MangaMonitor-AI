# A2 例外收尾：138 项已处理，正式库全部为 ZIP

2026-09-13。用户在上一轮保留原位决定后，明确要求尽量处理 154 项，并直接删除对应的 908 个原文件／目录。此最新授权覆盖原先等 B 迁移后再处理的安排。删除与例外整理均已执行成功；原先未执行的移出记录单独保留为历史。

## 实际结果

- 先重新验证 908 个原始对象及其 937 个替代 ZIP，再按固定清单删除原件。删除 26,560 个文件和 1,130 个目录，17,131,904,533 字节，0 失败；937 个 ZIP 保留，根目录核对符合预期。
- 154 个例外条目处理 138 个：62 个输入转 ZIP、69 个 ZIP 调整外名、5 个已有 ZIP 保留、2 个完全重复 RAR 复用现有 ZIP。转换核对 3,195 个文件，图片未重编码，已有章节和元数据保留；改名 ZIP 完整哈希不变。
- 5 组同名文件通过成员 SHA-256 和图片顺序比较：2 组完全相同，3 组内容不同并各自保留。没有只按名称或 CRC 合并内容。
- 这轮另外产生的 64 份原目录／RAR 约 1.08 GB 移到库外可恢复备份；它们不属于前述 908 项物理删除清单。实际执行 138/138 成功，0 审计差异。
- 正式漫画库根目录现有 2,961 个条目，全部为 ZIP。该数量是文件容器数，不是全库跨来源唯一作品或全量视觉完整性证明。
- 剩余 16 项原样保留：12 个 ZIP 缺可靠作者信息，可以保留原名以后补全；4 个 ZIP 内有 7 张转换前就存在的零字节图片。本次本地内容候选检查未找到可证明对应的替代副本，没有联网补档或猜造作者。

本轮依据现有明确署名、原包元数据、库内相同署名和用户新增作者确认调整名称。作者确认及私人文件名、页码、源 ID、完整清单和内容哈希仅留本机，不写入仓库。

## 验证与交接

本轮 7 项独立维护工具检查通过，覆盖源变化阻止执行、目标变化／覆盖保护、精确删除范围、原件备份和内容保留。删除前重新检查了替代包和原件，138 项实际应用后完成最终内容与目录核对。此前 A1 五本样例已获用户视觉验收；没有宣称全库已视觉验收。

合并路径表有 1,075 条记录，对应 1,073 个 ZIP 目标，保留已有来源信息并标明原件已删除、改名、移至备份或原包复用。此表是 B 迁移输入，不是全库库存，不自动推断未知来源。应用索引和下载历史仍可能记录已退出的旧路径，B 必须接入映射后完成关联迁移。

最新本机证据为 `MangaMonitor-library-zip-20260913/A2-delete-20260913`、`A2-review-20260913`、`A2-review-applied-20260913`。合并表在最后一个目录的 `path-mapping-for-B.json`。用户报告和剩余 16 项清单位于 `MangaMonitor-deliverables/batch-library-exceptions-2026-09-13`。不要重跑已完成 A1/A2 或删除／例外应用计划；旧的原位保留决定已被最新授权覆盖，但其历史记录不得改写。

## 当前位置

**A 的存量 ZIP 格式整理已完成，16 项元数据／旧档损坏问题登记保留；下一批 B 尚未实施。** B 要统一电脑实际文件的入库判定、JM/Pica 新 ZIP 输出、路径关联迁移，并退出手机名单和分类书单。此轮没有修改应用代码、私有索引／任务数据、安装版本或生产状态，没有来源请求、真实下载、应用 CI／编译、安装包、提交推送或 GitHub 写入。

后续顺序维持 B → C 版本关系与作者补全 → D JM 每周必看及 Pica 排行榜 → E 发现与完整范围批量 → F 原云端库存／完成状态闭环 → G 界面设置、V1 验收及安装包。阅读器／应用内更新冻结至 V1 后；PDF/CBZ 导出和分类书单取消。见 [完整路线图](ROADMAP_ZIP_LIBRARY_V1_2026-09-13.md)。
