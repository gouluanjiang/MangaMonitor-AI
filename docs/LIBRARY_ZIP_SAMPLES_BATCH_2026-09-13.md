# A 批：存量 ZIP 整理工具与样例

2026-09-13 A1 历史记录。用户已明确验收通过五本样例；后续 937 个无争议项的转换和当前原件保留状态见 [A2 报告](LIBRARY_ZIP_BATCH_2026-09-13.md)。下面保留 A1 交付时的实施证据，不能把样例通过当作整库完成。

## 已完成

- 在本机读取当前库的文件表、目录边界与必要作者元数据，复用已验收的 USB 迁移。两个集合目录按独立作品规划，不整组打成一本。
- 本机维护工具支持目录/RAR 转普通 stored ZIP，图片不重新编码、文件内容与章节结构保留；已有 ZIP 可原字节保留。输出不覆盖已有目标，不改应用的库存或下载记录，不发起网络请求。
- 元数据确认的社团/作者可移到标题前，保留语言、版本与整理/汉化说明；生成的来源编号与开头展会编号移除，源元数据不改。未知作者与目标同名保留待复核。
- 完成 12 项有针对性的本地检查；RAR 合成样例的递归打包参数修正后该项通过，后续只重跑发生变化的相关检查。没有重复执行应用本地/CI套件。
- 生成 5 个真实独立样例，108 个文件逐项校验通过；其中 104 个图片文件含已有封面。现有 ZIP 样例的整个文件哈希保持一致。原库顶层清单和源文件复核保持，未替换或删除原件。

本批是独立的本机维护工具与真实文件操作，不是桌面应用改版。未修改下载协议、库索引/队列语义或生产权限；未来应用 ZIP 最终输出仍属于 B 批，执行时按对应契约评审。

## 交付与状态

工具、计划、输入问题清单、逐文件哈希和用户报告留在 Codex workspace 下的 `MangaMonitor-library-zip-20260913` 与 `MangaMonitor-deliverables/batch-library-zip-2026-09-13`。其中包含私人库文件名，不进入公共仓库。用户确认的集合作者信息留在该本机交接，不需要再次询问。

当前样例入口只允许有限的独立样例，不可当作已授权的全库执行器重放；每个完成的样例计划只运行一次。五本样例实际阅读已由用户验收通过；这不代表其余作品都做了视觉阅读检查。

按既定路线，用户验收样例后进入 A2：刷新源清单、明确重名/未知作者项，复用转换与核对核心实现获批批量流程，逐项验证；旧格式退出在结果核对后按明确范围安排。不得为合并同名而删除不同内容的作品，不以格式转换掩盖原输入中的空图片。

已发现实际存在的目标名称冲突、原 ZIP 空图片与作者位置不明确项，均有本地清单；异常不会被自动覆盖、猜配或联网修复。正常 ZIP 无需全部重压。

当前未新增安装包、未运行应用 CI/构建、未提交推送。保持现有未提交文档/规则修改，不覆盖其他任务的改动。

## 后续路线

A1 样例已由用户验收 → A2 无争议项转换已完成、原件退出与例外待收尾 → B 电脑唯一漫画库及两源 ZIP 输出、取消旧名单/分类书单 → C 版本与作者補全 → D JM 每周必看/Pica 排行榜 → E 发现与完整收藏补齐 → F 原云端状态闭环 → G 正式 V1。阅读器/更新继续为 V1 后讨论，PDF/CBZ 导出及分类书单取消。
