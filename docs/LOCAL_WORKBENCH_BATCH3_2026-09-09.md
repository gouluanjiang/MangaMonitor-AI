# 第三批开发记录：真实账号与来源资料

日期：2026-09-09。分支：`codex/local-workbench-preview`，沿用草稿 PR #19。Windows 开发版版本：0.3.0。

当前状态：代码实现、自动化验证和开发安装包交付已完成，进入真实账号验收。尚未接收或使用用户凭据，不能把合成协议或 UI 测试标成真实账号通过。

## 功能与边界

| 功能 | 本批实现 | 验收边界 |
| --- | --- | --- |
| 两源登录 | 设置中分别连接 JM / 哔咔，验证返回的账号资料后显示连接成功，支持退出和重新登录 | 密码只用于本次登录；真实网络行为仍需用户账号验证 |
| 保留登录 | 可选择把服务端会话存入 Windows 凭据管理器；下次启动重新验证会话 | 密码不落盘；系统存储失败不回退明文；失效记录保留供重试 |
| 切换账号 | 会话代际隔离，旧响应丢弃；凭据比较与修改受跨实例互斥锁保护 | 冲突提示重新读取，旧实例退出不会删除新实例会话 |
| 在线收藏 | JM 收藏文件夹、JM/Pica 分页收藏与显式刷新 | 每次用户操作最多请求一页，不自动完整扫描 |
| 发现与详情 | 关键词搜索、作品 ID 或受支持链接解析、单作品详情、受限封面 | 原链接仅提取 ID，不让前端指定请求域名；真实库存与下载状态保持未接入 |
| 收藏操作 | 从作品详情收藏/取消收藏，先读取、最多提交一次切换、再读取核实 | 响应不明时保持未确认，重新核实前禁止重复提交；尚未操作真实收藏 |
| 本机关注 | 关注作品与作者名称、取消关注，按来源及远端账号隔离保存 | 本机关注可手动查看作品/按作者名称搜索；没有伪装成网站关注或自动监控 |
| 真实作品书单 | 真实详情/多选加入既有书单，两源引用同时存在；已读资料可回到详情 | 书单仅保存来源和 ID；缺失资料关联保留，一次显式读取最多 20 部 |
| 内存与错误处理 | 元数据、页数、封面解码与并发有界，断网/限流/403/过期分别保留状态 | 缺失页数、章节数和分页末尾证据仍为未知，不填 0 或宣称完成 |

已有本地漫画库和队列仍是明确标注的演示数据；设置、书单和本机关注保存是真实本机文件操作。新账号来源页使用独立 DTO，不能将真实作品加入演示下载任务。

## 存储与权限

- Windows 会话槽位：当前 Windows 用户的 `MangaMonitor/WorkbenchPreview/v1/Accounts/{JM|Pica}`，不枚举其他凭据。
- 本机关注：应用数据目录下 `workbench-preview-v1/following.json`，沿用版本、CAS、跨进程锁和原子替换；损坏/未来版本保留，拒绝覆盖。
- 关注账号键为来源与远端账号 ID 的散列。文档不是加密数据库；具有当前用户文件访问权限的程序可以读取本机关联。
- 主窗口增加八条专用 `source_*` 命令。原生客户端控制固定 API/CDN、请求预算、会话和文件位置，前端没有通用网络、路径或进程接口。
- 登录会话和密码不会写入书单、关注、日志、报告、仓库或 CI 工件。CI 使用合成凭据，真实来源请求被禁止。

## 验证分层

1. 合成协议用例：登录/恢复、分页、签名/解密、详情身份、单次收藏切换与结果核实、封面与数据边界。
2. 账号服务与本机存储：失效/切号、外部凭据更新、跨实例 CAS、关注隔离与冲突、旧封面响应拒绝。
3. Windows 凭据管理器：仅在临时 CI 用户中写随机专用测试槽位，验证读回、更新/删除、跨进程互斥与冲突；清理仅测试自身槽位。
4. 前端 Node 与 Chromium：原生 IPC 模拟覆盖两源切换、旧响应、登录错误、书单与关注冲突；这些不是实际账号验证。
5. Windows 实际 WebView：构建后的真实窗口、IPC 拒绝合成登录、密码清空、原生文档保存和重启读取。
6. 用户真实账号：待开发包通过以上检查后，在本机应用中输入；先登录、读取少量收藏/搜索/详情，再由用户在界面选择要验证的收藏操作。

本批没有进行本地重编译或全量测试。完整编译、跨平台测试、Clippy、WebView 与打包交给 GitHub Actions；本机仅做格式与依赖锁等轻量检查。

## 尚未完成

真实账号登录、会话重启恢复、当前域名及 CDN 可达性、真实收藏列表和用户选定的收藏操作，必须取得实际结果后才能标为验收通过。协议参考与合成测试不能替代这些证据。

真实漫画库/库存对照、下载队列、ZIP 与入库、生产监控/同步、自动作者更新以及 V1 端到端验收属于后续批次。`production_enabled=false` 保持不变。


## 已完成的技术验证

验证提交：`0bdfcda3896c4a829c5cff3a2aaa2c414a09a88f`。CI 实际 PR 合并提交：`36ba43f666f11d4b18d0c4f3338c1a84c8e48b0c`；基线 main：`69fcf1dd24526b5920406c9b97bb3e8b8909c6c6`。

| 验证 | 结果与证据 |
| --- | --- |
| 前端 | [UI CI](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34335090194) 成功：56 个 Node 测试、54 个 Chromium 回归、格式与 TypeScript/Vite 构建 |
| Rust 基线 | [Baseline CI](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34335090165) 成功：526 个 Linux workspace 测试、63 个 Windows 核心测试，CLI 构建、Clippy 与生产门禁检查 |
| Windows 模块 | [Desktop CI](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34335090095) 成功：18 个账号服务、14 个凭据、26 个来源协议/封面、30 个存储测试 |
| Windows 安全存储 | 同一 Desktop CI 显式运行随机测试槽位的真实凭据回读/CAS/删除，以及子进程竞争锁；受 CI 限制的测试并未留在 ignored 状态跳过 |
| 原生应用 | 15 个原生 IPC 测试、Clippy、NSIS 打包、实际 WebView 登录拒绝/密码清空、文档写入、关闭进程及重启读取通过 |

首次原生 smoke 因旧脚本从漫画库寻找未入库演示作品而失败。修复测试后，流程从已入库样例创建书单，再通过实际原生 IPC 加入一条合成未知引用，确认重启后保留且不提供下载入口。没有为使测试通过放宽应用权限或下载资格。上传步骤也改为只选择当前版本安装包，排除缓存中的旧版本。

## 实际界面复核

主审查者使用 CI 构建的前端工件，在仅监听 127.0.0.1 的独立核对目录运行；数据来自明确标记的合成 IPC（100 部可区分作品），没有连接真实网站。临时核对脚本与本机背景图均不进入仓库、CI 或安装包。安装包的原生行为另由 Windows 实际 WebView 检查覆盖，不能把浏览器核对称作本机安装验收。

基准内容区为 1672×941，浏览器缩放 100%；另查 900×720 登录窗口及 390×844 来源搜索。比较采用默认 B、A 全窗背景、5/7/9 密度，具体结果如下。

| 页面 | 已打开的批准依据 | 实际结果 |
| --- | --- | --- |
| 公共布局与背景 | B-banner-7.png、A-dark-5.png、A-dark-9.png | 沿用窄导航、顶部背景与暗色/紫色操作；个人图仅在本机核对 |
| JM / 哔咔收藏 | favorites-jm-selected.png、favorites-pica-browse.png | 两源切换、JM 已有收藏夹、普通浏览无勾选框；100 部全选包含未进入视口的记录，末行与底栏可达 |
| 来源详情 | detail-01-not-imported-v2.png | 修复左封面偏小及简介错落左侧：桌面大封面与右列简介/来源信息恢复；未知页数、章节、库存与未接入下载如实保留 |
| 账号与登录 | settings-accounts.png；登录表单继承公共样式 | 左侧分类、两源独立连接块、断开引导和居中登录窗口可达；窄窗不挤出提交按钮 |
| 关注 | following-authors.png、following-works.png及删除头像文字修正 | 作者行无头像/首字圈占位，作品使用网格；本机关联与未启用自动监控有明确说明 |

目视检查发现来源搜索错误地靠左、列表顶部留白偏大和详情左右分量偏差，已在 0bdfcda 修复并复查 CI 运行工件。新增几何回归覆盖三个来源入口的右上搜索、桌面详情尺寸与右列简介、390px 窄窗不溢出。真实账号内容、真实封面可达性及用户机器原生安装仍待验收。

## 安装包与真实账号入口

[已验证的 0.3.0 Windows x64 安装包工件](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34335090095/artifacts/10097535446)，包含且仅包含 `MangaMonitor Dev_0.3.0_x64-setup.exe`。

- 安装包大小：3,088,717 字节；版本信息：0.3.0；签名状态：NotSigned（开发包）。
- 下载 ZIP SHA-256：`8440007719f93cae214ae15a405ea25b4741881a7629daafd975d7aa4b7cd537`，已与 GitHub Actions 工件摘要核对。
- 安装包 SHA-256：`eeb65b7d4c26fe3fbd7bc200f5f54a4f7ec4a41bb8a924464243ff41b9b3f524`。
- 本机交付目录：`MangaMonitor-deliverables/batch3-2026-09-09`。已下载和检查，没有自动安装或替用户登录。

运行安装包后，在 **设置 → 账号与收藏 → 连接 JM / 哔咔** 输入账号密码；只有需要重启保留时才勾选“记住会话”。先验证登录和少量只读资料，再由用户选择要收藏/取消收藏的作品。无需把密码发到聊天、写进配置文件或设置 GitHub Secrets。

当前位置：第三批代码及自动化验证完成，真实账号验收待进行。下一开发批为真实漫画库和库存对照，不能把目前的演示漫画库当作实际库存依据。
