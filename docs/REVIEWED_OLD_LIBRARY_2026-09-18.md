# Explicit registration of the reviewed old library

### Library authors added to local following, 2026-09-19

The user explicitly requested extraction of all current library authors and addition to the application's followed-author lists. A private one-time review normalized explicit authorship, repaired known tokenizer fragments, separated named coauthors, and retained evidenced source spelling variants and circle-only credits. Unknown credits and generic/translation/character noise were not guessed into author identities. Complete per-name evidence, unresolved authorship notes and the plain-text list remain outside Git in `Documents/Codex/MangaMonitor-follow-library-authors-20260919`.

The prepared names were added to both existing account scopes after confirming the saved scope relationship, closing the app and backing up the private documents. The following document alone was updated with schema/name/capacity validation, exact prior hashes, a revision increment, the existing store lock and an atomic replacement retaining the original. Existing follows and work follows were preserved. The prior library, downloads and saved discovery documents remained byte-identical.

The unchanged verified executable restarted, both source following screens displayed the expected complete counts, and the author-update entry reflected the newly unchecked range while retaining its old results. No all-author check, media download, website follow mutation or runtime matching change was performed. The known initial local-following busy indication cleared through normal refresh; its cause is not claimed fixed. Some works lack reliable creator credits, and query names include necessary aliases rather than representing a count of distinct people. Do not replay this applied import or start a full-source scan solely to reproduce its evidence. No code, build, version, installer, release or production setting changed.


### User-directed file cleanup and local acceptance completed, 2026-09-19

The user authorized deletion of the identified empty ZIPs, accepted the designated creator-only files as local library works without further identifier lookup, and delegated the remaining file-treatment choices. Only the exact freshly verified empty archives were deleted. The creator-only works were already locally indexed; their accepted status does not invent website IDs or mark unrelated online results owned.

Archives containing zero-length image members were backed up outside the library and repaired by removing only those empty entries. All nonempty member payloads and metadata were retained and independently checked. A confirmed multi-volume work was consolidated into an ordered collection ZIP, retaining the original volume archives outside the library. Its first generated container was rejected by the native parser because prefixed member paths disagreed with inherited Unicode Path extra fields; those path-dependent fields and CRCs were rebuilt. The corrected container retained every original member payload and unrelated metadata and passed the actual native rescan. This is a file-packaging correction, not an application-code change or a page-completeness claim.

The final native directory rescan completed without file-review errors. A further pinned, add-only source registration was applied using the existing verified helper, with a fresh backup, whole-file hash verification, expected revision and exact delta audit. Existing source registrations, unrelated manga files, download history, following and saved author results were preserved. Bounded native checks passed for local availability, an added owned source record and the saved author-update counts; this is not a full-source crawl or a decode of every image in the library.

The known transient local-following busy indication cleared using the normal local-following refresh during acceptance; this does not claim a root-cause application fix.

The remaining source-identifier gaps are retained as optional evidence records for otherwise present local works, not an instruction for the user to re-identify every file. Neither local acceptance nor a missing source ID establishes a cross-source identity. Private counts, paths, titles, identifiers, backups and receipts remain under the private review directory's `project3-round3-20260919/user-decisions-20260919`. Previous transaction evidence is preserved. Do not replay the cleanup, rescan or import just to reproduce paperwork. No product code, build, installer, version, release, production setting or runtime automatic matching changed.


The user resumed post-V1 project 3, the old-library review, and asked to complete it. They also explicitly accepted the previously inspected set of 154 incomplete ZIPs as normal library works. That decision removes a file-completeness prerequisite for those exact files; it does not invent source identities or authorize deleting/replacing media.

The application previously counted only successful same-source download records and current files. Confirmed old works therefore remained unowned in favorites, author search and author updates. This change adds separately recorded old-library review evidence to that shared inventory projection. No successful download records are fabricated and no runtime title, author, translation or cross-source inference is restored.

## Import and persistence

- A local maintenance helper reads an explicitly reviewed JSON manifest containing the exact configured root, relative ZIP paths, byte sizes, SHA-256 hashes and individually reviewed source/work references. Real manifests and titles stay outside Git.
- Preview checks the scope, index and file identities and reports counts plus the manifest digest and current library revision. Its result explicitly says file hashes have not yet been verified.
- Applying requires that exact manifest digest and expected revision. The helper verifies every file hash and current identity, rejects conflicting/duplicate references and writes the reviewed evidence in one revision-checked private library transaction. It never writes manga bytes, changes legacy metadata links or updates the download queue.
- Registration accepts the reviewed ZIP as the user owns it; it does not assert that every chapter/page is complete. The raw private completeness findings remain available separately.
- Ordinary inventory reads verify the registered file identity and preserve source namespaces. Missing or replaced files stop counting as owned. Same-root rescans retain explicit registrations; changing roots does not inherit them. Existing verified path-migration evidence can locate an unchanged registered ZIP.

The helper is intentionally separate from ordinary application flows. It has no renderer IPC endpoint and refuses its real-store apply mode in CI. The application gains the owned status from the imported evidence, not a new interactive guessing/association workflow.

## Local usage

Run the exact new helper and application after CI passes. Back up the current private library document and preserve its revision. Close the previous application before changing its private metadata, then open the newly verified executable afterwards; older builds do not understand the added registration field.

```text
mangamonitor-library-review --app-data APP_DATA_ROOT --manifest REVIEWED_MANIFEST
mangamonitor-library-review --app-data APP_DATA_ROOT --manifest REVIEWED_MANIFEST --apply --expected-revision PREVIEW_REVISION --manifest-sha256 PREVIEW_DIGEST
```

Only the manifest's reviewed entries are imported. Ambiguous candidates are kept in a separate private review list and do not become owned just because they have a similar title. The result is not a claim that every old library work has a known source identifier.

## Verification status

Final head `6c8ea292af9eba6c13846f4b06993099df5bf8a2`, test merge `1c3f29d8e51122f8c4d0f1c3c3540b70c9dceba1`, passed baseline CI `35343829531`, frontend `35343829577` (149 logic / 116 Chromium) and desktop `35343829532` (Windows download module 66 tests, eight new library-review regressions, module/credential checks, 34 native IPC, Clippy, executable and actual CI WebView startup/restart). Initial runs exposed a dependency incorrectly limited to tests and an outdated nested lockfile; both were corrected before final validation. Formal suites and builds ran only in CI.

The application and maintenance-helper artifacts were checked against GitHub archive digests, ZIP CRC, PE format and their pinned revision. Application SHA-256: `c180a60ef7fec736d90f62091e09c88d60348fa4dfc7f5ae0fba6ee81138e5a8`; helper SHA-256: `4ddd5871566a12b5ae31e2a624bee81d892fd6ee3910d1c816f46632eb09d657`. They are delivered separately in `Documents/Codex/MangaMonitor-Dev-20260918-6c8ea29`; version remains 0.3.4.

After explicit user authorization, the old app was closed and the finalized private manifest was applied with its exact digest and expected library revision. Every selected ZIP was rehashed; all file identities and imported references were checked after the atomic write. Original library fields and the download-history, following and saved author-check documents remained unchanged. No manga file was modified. Known accepted partial ZIPs were eligible; unrelated empty ZIPs were held out. Private titles, identifiers, manifests, review counts and evidence stay outside Git.

The exact new executable starts and reads the updated store. After the user completed JM login, bounded native verification passed for both JM/Pica favorites, one fresh two-source author search and the existing saved author-update scope. Complete cached favorite counts agree with the private manifest; owned/unowned filters display the corresponding counts and card states. The fresh author query completed its full returned pagination, kept other keyword hits separate and recognized reviewed old ZIPs. The saved author-update view also immediately uses those registrations without a new all-followed-author scan. No download was selected or started. Library, download-history, following and saved author-check document hashes remained unchanged throughout this follow-up. Private UI evidence and exact counts remain outside Git.

Existing desktop and Start Menu shortcuts were backed up and updated to the verified executable; old executables remain. The agent restored the author-update page to its unowned view. This completes the bounded import/native acceptance for the applied subset; remaining private identity ambiguities still need review and are not silently promoted. The earlier V1 user acceptance is not replaced by this agent-run verification.

Synthetic coverage includes preview without mutation, separate download history, an accepted partial ZIP, atomic failure, scope/digest/revision checks, conflicting references, repeat import, rescans/root changes and removal/replacement. Ambiguous old-library items remain separate from applied registrations. This is not a statement that the entire old library has been identified. No production enablement, installer, release or new manga download is included.

## Additional private review and import, later on 2026-09-18

The user requested continued review until only unsupported identity decisions remained and expressly authorized the prepared title-fragment searches to the existing JM endpoint. The bounded queries completed. Previously saved source metadata, original names, archive structure, local OCR and individually reviewed chapter/language evidence supported another explicit manifest. The same verified maintenance helper previewed that manifest, then applied it with the exact digest and expected revision after checking every selected ZIP hash. An independent check verified every new reference and file identity, preservation of previous registrations and other library fields, and byte-identical download-history, following and saved author-check documents. The existing accepted-partial-ZIP policy remains limited to its explicitly accepted files. Confirmed same-work copies were recorded separately and retained; same work does not imply byte-identical images.

The same application executable was reopened. Native owned/unowned filtering passed against the currently loaded first JM page, the complete Pica favorite cache and the existing saved author-update scope. The JM page reported a temporarily unavailable local cache and loaded only its first page; its full saved catalog was checked offline, not claimed as a complete native replay in this continuation. That cache-read observation is not diagnosed or fixed by this data import. No new all-followed-author check or manga download ran. All four relevant private-store hashes remained unchanged during the native follow-up, and the saved author-update page was restored to unowned. No code, version, installer or CI rerun was needed.

The final private report, exact import/verification evidence and a searchable manual-review HTML page are under the private review directory's `project3-round2` subdirectory. The page separates unresolved source identity, chapter/file range, edition/language and empty/unindexable archives; candidate identifiers remain unconfirmed until supported. Exported user answers are review input, not an executable import manifest. Agent-created temporary OCR image files were removed after retaining text/hash evidence. Manga files, current download history and original metadata were preserved. Remaining cases are intentionally not promoted, and runtime automatic matching remains absent.

## Further private review, 2026-09-19

The user resumed the residual review and approved further bounded metadata queries. Local archive structure, existing naming records, saved source metadata and in-memory text recognition were independently reviewed. A further explicit subset was imported with the same verified helper, pinned manifest digest and current private-store revision after closing the application and backing up its current documents. Whole-ZIP hashes and the exact post-write delta passed; existing registrations, other library fields and the user's intervening download were preserved. No manga bytes or download history were changed. Scope notes for complete works contained within a larger ZIP remain in private sidecars; do not claim that every unassigned chapter in such a ZIP was resolved. Empty/unindexable archives, unsupported language versions and other undecided file scopes remain excluded. The previously accepted partial-archive policy was not extended to new files.

The existing executable restarted successfully. Saved author-update counts and owned/unowned filtering remained correct without a new all-followed-author check. The private library, download, following and saved-check documents stayed byte-identical after import during the native checks. Two Pica detail reads returned metadata but insufficient language evidence, so those candidate relations were not promoted. Both bounded fresh JM single-work detail samples returned successfully and displayed the expected owned status. The initial attempts used a JM prefix accepted by the download entry but rejected locally by the source lookup parser; repeating them with supported numeric input passed. This corrects the acceptance procedure, not application or source behavior, and does not constitute a new full-author or all-identifier scan.

Private decisions, applied-manifest verification, a searchable residual list and the report are in the private review directory's `project3-round3-20260919` subdirectory. Some remaining identities are already known and need a file-treatment decision instead of another identifier search. The user separately approved the two additional exact JM detail reads. Both completed; a small supplemental manifest was then applied and verified with a fresh backup and revision, followed by a successful restart of the existing executable. No approved identifier query remains pending. A final coverage audit also read previously returned, authorized candidates and checked a small number of local attribution pages. A further bounded supplement was backed up, hash-verified, applied and reopened; an added native detail sample displayed its expected owned status. The prior import snapshots and unrelated private documents were preserved. The two bounded native JM source-detail samples subsequently passed using supported numeric input, as described above. Do not repeat the applied import or discard intervening user state. Runtime automatic matching remains absent. No code, version, installer, CI rerun, push, release or production change was made for this data/document-only continuation.

## User-approved additional partial archives, 2026-09-19

The user explicitly accepted the separately enumerated remaining partial ZIPs under the same existing-file policy as the earlier accepted set. Only that exact list was applied. Existing known identities were rechecked; accepting missing pages did not invent identities for other chapters or language versions. The existing helper imported the pinned manifest after a fresh backup and revision check, verified whole-file hashes, and preserved all previous registrations and unrelated private-store documents. Original missing-page findings remain in private evidence; no manga bytes or download history changed. Empty and unindexable archives remain outside this authorization.

The same executable restarted successfully, an added owned-detail sample passed, and existing saved author results retained their owned/unowned filters without a fresh full-author scan. The current private aggregate and residual list were updated; complete transaction snapshots were retained. No code, build, installer, release, production change or runtime matching was introduced. Do not repeat this import. The private `project3-round3-20260919/accepted16-20260919` directory contains the applied manifest, exact audit, backup and report.

## Page-independent review continued, 2026-09-19

The user clarified that some pages were deliberately removed and that page counts must not determine identity. The residual old-library review continued using explicit authorship, work/volume titles, copyright/translation attribution and existing source evidence. Page equality was not treated as identity proof; page differences and gaps no longer blocked otherwise confirmed identities. An additional pinned add-only manifest was imported with whole-file hashes, a fresh store backup and exact revision/delta checks. All previous registrations and unrelated store fields/documents were preserved. No manga bytes, downloads, application code, version, production setting or runtime matching behavior changed. The current private residual list distinguishes actual identity/volume ambiguity from empty files and unindexable zero-byte image members. Do not repeat the import; see private `project3-round3-20260919/page-independent-20260919` evidence.

Bounded native acceptance for this page-independent import passed on the existing executable: one newly registered item per source displayed owned and was excluded by the unowned filter; complete favorite caches and saved author results showed valid inventory counts without a new all-author scan or download. All four post-import private-store document hashes remained unchanged. Startup initially showed unknown inventory; normal navigation later recovered it without data or code changes. The cause of that transient was not established and is not claimed fixed. This is not an all-identifier native test.

## Visual identity supplement imported, 2026-09-19

A bounded comparison of existing local cover/title pages with covers displayed by the native source-detail view resolved further explicit old-library identities. The independently pinned supplement was applied and verified with whole-file hashes, a fresh backup and exact registration-delta checks. Earlier transaction evidence was preserved, including its distinct native acceptance record. Existing registrations and unrelated application data remained unchanged; no manga files, application code or runtime matching behavior changed.

Post-import bounded native acceptance for this supplement passed separately: a newly registered source-detail sample showed owned after restarting the existing executable, and saved author-update counts remained valid. Private library, download, following and saved-check document hashes stayed unchanged. A transient local-following busy warning cleared with its normal refresh; its cause is not claimed fixed. This does not constitute an all-identifier scan. Private titles, identifiers, counts and source images remain outside Git. Do not repeat the applied import.
