# 来源搜索与关注名单作者入口补修 · 2026-09-17

## 问题与根因

用户在 Pica 来源搜索作者时仍看到了上次误选的其他作者作品。`44da160` 的修复只接入独立作者搜索和作者更新；`SourceWorkbench` 的来源搜索以及关注名单“搜索该作者”仍直接显示通用关键词结果。因此不是同一判断再次失效，而是调用入口覆盖不完整。

## 修正后的行为

- 来源搜索默认“按作者搜索”。来源作者字段明确对应查询名的作品进入默认列表、入库统计与全选；社团括号、合著与 Unicode 格式规则复用同一函数。
- 保留“作品关键词搜索”：需要标题等广泛匹配时明确选用，保留来源的全部命中并说明不代表同一作者。单个编号／链接继续直达详情。
- 关注名单“搜索该作者”固定使用作者规则，不再走没有作者依据的列表。
- 其他关键词命中独立可查看，保留实际作者或缺失说明；该区不提供多选／批量下载。切换结果区或搜索方式清空临时选择，不把原选择带入另一个范围。
- 仍读取完整原始分页；首页只有非作者命中时也继续读后续页。原始来源总数和作者作品数分别显示，不通过过滤修改来源报告数量。
- 完整作者全部入库提示还要求没有未确认命中，不能只凭筛选后已入库结果宣称全部齐全。详情查看、来源请求参数、账号作用域、下载执行和入库契约均保持原有规则。

## 验证安排

新增四个 Chromium 场景：JM 作者模式、Pica 作者模式、显式关键词／编号模式、关注名单作者入口。合成两页数据把全部非作者命中放在首页，作者作品放在下一页，覆盖完整读取、作者统计、作者全选、其他结果不可多选、切换清空和通用搜索仍保留全部命中。真实作品、作者名字、编号或账号不进入回归数据。

首轮 CI 的 149 条逻辑检查与前端构建通过，Chromium 为 113 通过／2 失败：两条新作者场景使用默认“未配置漫画库”夹具，却断言“未入库”数量。补齐合成空漫画库和下载库存读取响应，保留原统计断言，之后复跑当前修订；没有为通过测试把未知状态当成未入库，也没有变更产品入库规则。

修订 `49c40e843009ca968b595e0fa5009fc096b949cd` 工程验证通过，PR 测试合并为 `e723b3a97180d471e600c96f95768b3d562cebcd`：

| 检查 | 实际结果 |
| --- | --- |
| [前端 CI](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/35236666931) | 格式、149 条逻辑、类型／构建、115 条 Chromium 全部通过；包括新增四个场景。 |
| [基础 CI](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/35236666896) | Rust 工作区与 Windows 基础检查通过。 |
| [桌面 CI](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/35236666854) | 模块／凭据、34 条原生 IPC、Clippy、EXE 构建及 CI Windows WebView 启动／重启通过；安装包步骤跳过。 |
| 独立工件核验 | [Dev 工件](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/35236666854/artifacts/10504511036) 的 GitHub 摘要、ZIP CRC、PE 标识和内嵌提交均核验通过。 |
| 合成页面审阅 | 查看本次 CI 生成的作者列表与其他结果两张截图，对照已接受公共布局；标题、右上搜索、查询方式、分区说明与筛选无重叠。1280×720 截图中卡片仅露出顶部，其余需滚动，不把截图当作完整列表或实机证明。 |

正式套件和构建仅在 CI 运行。交付位置为 `Documents/Codex/MangaMonitor-Dev-20260917-49c40e8`，仍为 0.3.4 的独立 EXE，没有重装。ZIP SHA-256 `0e44feb2f1f4144a01cec28954e536c9c5a069797720b411b178e2f732bd9c73`；EXE SHA-256 `6353f05ad2ac3a6b49c8a76af245a30dba27dabb2d53d1f31fcdcfc86c274176`。用户随后允许恢复 Computer Use；已按确切路径启动修正版并重新核对 EXE 哈希，补充以下真实来源验收。

本机另用此前保存的同一问题查询做了只读离线投影：JM 15 条仍为 15 条作者作品；Pica 12 条分为 6 条作者作品、6 条其他命中。新的来源入口与既有作者面板结果一致，误下载样本没有进入作者作品。原始记录全部保留，私人存储字节未变；这是缓存元数据诊断，不是新的在线查询或原生交互验收。

## 恢复窗口操作后的实机验收

- Pica 同一问题查询实际读完 12 条：默认作者列表为 6 条，其他关键词结果为 6 条。逐条查看默认列表的来源作者字段，误下载样本不在其中。
- 默认作者列表全选为 6 条；切到其他结果显示选择已清空，不提供多选／批量操作。误下载样本仅在其他结果中，实际显示未入库。返回作者列表也没有残留选择。
- 显式作品关键词模式读完全部 12 条，并显示不代表同一作者的范围提示。切换模式先清空旧结果，重新提交后才读取，不混用旧范围。
- Pica 本机关注读取初次出现繁忙提示，一次有间隔的重试后正常，当前名单为空。用户随后完成 JM 登录；其本机关注名单首次读取也出现繁忙提示，一次手动重试后恢复。具体原因尚未单独确定，不能将重试恢复写成根因修复。
- JM 从现有关注名单点击“搜索该作者”，完整返回 15 条，15 条均有明确对应的来源作者字段，其他关键词结果为 0。已核对 15 条作者标签、范围提示和完整读取状态；全选为 15，切换其他结果后清空选择并隐藏批量操作，返回作者列表也没有残留选择。
- JM 补验前后，漫画库、下载历史、保存的作者检查与关注名单四份文档哈希一致。此次补修的限定原生验收完成；用户随后于 2026-09-17 明确确认 V1 最终体验验收通过。
- 本轮没有准备下载、启动下载、添加关注或改动网站收藏。私人样本证据保留在 Git 外，公开文档仅记录数量和行为。

## 误下载样本与设置补验

用户明确要求删除先前误下载的单本 ZIP。此前已核对确切路径、元数据来源编号、长度和 SHA-256，仅删除该 ZIP。本次使用程序正常的“刷新漫画库”完成索引刷新；只读核验确认文件仍不存在、对应旧索引记录为 0，在线结果也实际显示未入库。没有直接改写私人存储。下载历史与保存的作者检查文档在本次验收前后哈希一致；没有新增下载或再次删除文件。详细凭据留在 Git 外。

设置关键词实际检查已完成：用户代填“记住会话”，代理确认只显示账号分类，清空后五项分类恢复。该项沿用已确认结果，无需因恢复 Computer Use 再做一次。

用户已确认 V1 最终体验验收通过。Dev 仍为 0.3.4，不生成安装包；修正版已通过工程验证、限定实机检查并单独交付。当前 PR #19 保持草稿、未合并，`production_enabled=false`。作者识别限于明确来源字段，不保证别名、缺失元数据和上游字段错误都能自动确认。
