# 漫画库操作与设置收尾

本批从 `729c415` 接续。上一批作者更新过滤修复、原 50 位与新增 50 位来源抽查已完成；本批不重复抽查，不把关键词结果当作已核实作者书目。

**本批开发与工程检查完成，交付提交 `4538fa6`，Dev 0.3.4。下一步是 V1 综合候选版与连续流程验收。** 本批新增入口的真实用户验收仍待完成。

## 本批实现

- 漫画库详情增加“打开文件位置”。原生端接收当前库和作品标识，从已有索引核对文件身份后，在 Windows 文件资源管理器中定位该文件或目录。网页不能传入任意路径；文件移除、替换或目录切换后明确提示重新核对，不执行漫画文件。
- “网络与诊断”显示当前账号会话、漫画库、下载队列和设置状态，并提供账号管理、漫画库设置、队列入口。会话状态不是新完成的网络连通性测试；重新读取账号沿用现有会话读取，不新增网站探测、作者扫描或后台检查。
- 可复制只含版本、状态和数量的诊断摘要。按允许字段构造，不序列化账号、会话、作品或路径；复制不可用时保留可手动复制的文字。
- 版本信息显示实际原生应用版本与构建编号。CI 构建将本次分支提交写入程序，便于区分同为 Dev 0.3.4 的交付文件。
- 设置记住本次使用的分类，各页面的账号入口直接打开账号分类；诊断页能直接进入漫画库设置。搜索支持“壁纸”“记住会话”“版本”等控件关键词，自动显示匹配分类；无结果时不保留无关表单，切换分类仍保留未保存外观草稿。
- 设置数据读取失败时，原生诊断和账号／漫画库入口仍可访问，外观编辑和保存保持不可用，避免用默认值覆盖未读取设置。
- 清理本地书单、待接入自动监控、人工关联等已取消功能的残留提示，保持实际运行语义。

当前下载并发、队列容量、ZIP、图片请求、入库判定和来源搜索范围均沿用既有实现；本批没有修改下载执行契约。程序内作者／漫画身份匹配仍取消，存量辅助核对留到 V1 后。

## 检查与边界

新增合成回归覆盖：只定位当前作品；错误库代次、未知标识、修改／删除后的文件不交给定位操作；新命令的主窗口和来源权限；原生版本返回字段；诊断摘要不带私人字段；复制失败可手动处理；设置快捷入口、关键词搜索、草稿保留和设置失败时的诊断可达性。

最终提交 `4538fa681bdf76c54599031fedce93b8ba0bf1c4`，PR 测试合并 `323fe352a2915975b69f5fbc9448234592d08455`，以下正式检查均只在 CI 执行：

| 检查 | 结果 |
| --- | --- |
| [前端 CI](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34852110866) | 145 项逻辑测试、104 项 Chromium 页面测试全部通过，格式、类型与构建通过；包括连写“记住会话”的搜索回归。 |
| [基础 CI](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34852110854) | Rust 工作区与 Windows 核心回归通过，生产开关保持关闭。 |
| [Windows 桌面 CI](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34852110870) | 模块及凭据检查、34 项原生命令测试、代码检查、Dev 构建、实际窗口启动／重启通过。含中文、日文、逗号的文件／目录通过真实 Shell 路径解析检查；安装包步骤跳过。 |

已打开公共布局 `B-banner-7.png`、详情 `detail-02-imported-v2.png` 和设置 `settings-accounts.png` 基准，并目视检查 CI 的详情、文件位置反馈、诊断页及复制失败状态。保留详情左右构图、紫色主按钮、设置二级分类和后来取消项的删减；修正了诊断分组间距。所检截图来自 `b545374`；最终 `4538fa6` 只补搜索别名和回归断言，未改变这些页面的结构或样式，最终 104 项页面回归也全部通过。合成截图不代表真实库封面或系统文件资源管理器的用户验收。

首轮 `2db9f5b` 的前端与核心检查通过，Windows 实际路径解析回归发现规范化路径的 `\\?\` 前缀不能直接交给 Shell。原生交界处改用已在依赖锁中固定的 `dunce 1.0.5` 进行不改变路径含义的格式转换；特殊保留名称无法安全转换时保留原样，系统无法解析时报告失败，不简单删除前缀。原有合成中文／日文／逗号文件及目录回归通过，实际文件位置核对仍由用户完成。参考 [dunce 的路径兼容说明](https://docs.rs/dunce/1.0.5/dunce/fn.simplified.html)。

不扫描或整理实际漫画库，不操作真实账号登录／关注，不下载或删除漫画。本批没有生成安装包或自动启动 Dev。Windows 系统文件管理器的实际定位结果和真实桌面综合流程仍由用户验收；合成回归与工程检查不代替这一项。

## 交付与验收

程序位于本地 `Documents\Codex\MangaMonitor-Dev-20260914-4538fa6\mangamonitor-workbench-preview.exe`；同目录附本报告和验收说明。[CI 原始程序包](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34852110870/artifacts/10352260006) 已校验 ZIP CRC、GitHub 摘要和内嵌构建编号。程序大小 20,351,488 字节，文件版本与产品版本均为 0.3.4。

- ZIP SHA-256：`9ea4fd4bb974a2c4d92b9f19a3eae188444017cf57e823cfbb237080e726cb3d`
- EXE SHA-256：`d94ff0906adfd354c0caeee439d8626620ef1f1c469feb6791fe7707a6a26b2a`

退出旧版程序后打开此 EXE，在“设置 → 网络与诊断”确认 `0.3.4 · 4538fa6`，然后检查三项：

1. 漫画库打开一本已存在作品，点击“打开文件位置”，应在文件资源管理器中选中对应 ZIP 或作品目录。
2. 设置中依次搜索“壁纸”“记住会话”“版本”，应进入外观、账号与收藏、网络与诊断。清空搜索后分类恢复；诊断页的设置快捷按钮进入正确分类。
3. 复制诊断摘要并粘贴到文本编辑器，应只含版本、状态和数量。若自动复制不可用，下方文字可手动复制。

开发分支及正式项目目录已同步到 `4538fa6`，原有三份最终报告已核对保留。草稿 PR #19 未合并；本报告的最终检查和交付证据在本地补齐，随下次必要代码推送归档，不另触发一轮仅文档 CI。

## 路线位置

阶段 1–3 与榜单 → 完整范围批量选书 → 作者更新修复与 50＋50 抽查 → **本批：漫画库操作、设置与诊断工程完成** → **当前位置：进入 V1 综合候选版验收**。

| 接续阶段 | 工作与完成条件 |
| --- | --- |
| V1 综合候选版 | 集中复核手动作者检查、完整分页、选书下载、ZIP 保存、同站状态同步、任意作者搜索和筛选；同时确认收藏写入与本批三个实际入口。已完成的抽查与数据整理不重做。 |
| 验收修正与 V1 确认 | 按连续使用中实际发现的问题修正，保留范围与失败提示；真实流程验收通过后再确认 V1 完成，不能以 CI 通过替代。 |
| V1 之后 | 单独讨论旧漫画库的人机辅助核对、阅读器及应用内更新。不会自动恢复其他已经取消的功能。 |

综合验收继续覆盖：手动检查作者更新、选书下载、ZIP 成功入库、任意作者完整搜索、已入库／未入库筛选，以及收藏写入、实际文件位置和剩余界面一致性。对 Pica 关键词精度、未登记旧库和跨站同本保留既有范围说明。阅读器、应用内更新在 V1 后讨论；不恢复其他取消功能。
