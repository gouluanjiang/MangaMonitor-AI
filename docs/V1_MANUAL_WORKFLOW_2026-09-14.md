# V1 手动使用流程开发记录

本报告保留 `6f794fd` 主体开发与交付证据。后续 `524f256` 的 JM 周榜空标题修复、全部 CI 结果及新 Dev 文件见 [周榜修复记录](JM_WEEKLY_FIX_2026-09-14.md)。用户最新决定将现有旧漫画的辅助核对留到 V1 完成后，当前不恢复自动匹配、不操作私人漫画库。

用户最新要求已恢复代码开发。按 `V1_SCOPE_MANUAL_UPDATES_2026-09-14.md` 推进，不再等待存量漫画关联。本轮不整理、扫描或下载真实漫画，不改变生产状态。

## 实现范围

- 在线入库状态改为同来源编号的已完成下载凭据和实际文件。新增只读状态接口，独立于图片进度轮询；切换页面、窗口返回、库变化和新下载完成时刷新。
- 清空队列历史保留的精简完成凭据继续参与状态核对；路径整理迁移证据继续有效。旧漫画元数据、标题和人工关系不作为下载成功依据。
- 收藏、来源搜索、详情、作者结果共用状态与筛选。移除自动身份推断、人工关联、跨站配对、语言版本补齐和自动下载入口及对应运行代码。旧私人文档原样保留但不参与判定。
- 单来源搜索自动读完分页；双来源作者搜索不要求先关注。读取中是临时数量，停止、短页、重复页、错误和上限不宣称范围完成。大结果使用已有虚拟封面网格。
- 作者更新只在点击按钮后检查。读取上次结果不会检查；检查中可停止。旧未下载结果保留，选择下载沿用已验收的准备与确认流程。
- JM 每周必看支持期数／类型，哔咔支持周榜／日榜／月榜。复用当前来源账号、运行内封面、入库筛选、详情与明确下载操作。榜单 API 没有分页时，不伪造“下一页”；JM 返回数量不完整时显示未确认。
- 作者搜索与榜单详情返回保留原查询和选择；文本筛选不能让部分已入库作品冒充整个作者已经补齐。
- 当前每批最多 50 本的下载能力保留。更大完整范围的选书和内部批次调度仍是阶段 4 后续项目，不能将本轮交付写成 V1 全部完成。

## 下载契约复核

已复读 `DOWNLOAD_EXECUTOR_THAW_GATE.md` 及当前桌面下载实现。本次收缩身份判断与自动下载权限，没有更换 JM/Pica/Python 参考版本、下载协议、图片并发或完成定义，复用此前 ZIP/JM/Pica 下载审查证据。新榜单只读路由的参考版本与检查记录见 `upstream-source-reference.md`。

下载完成仍要求来源与作品编号、会话、任务修订、库根身份、输出清单和已验证文件一致。已有目标不覆盖，下载失败不计入库，不自动删除旧包。移除人工关系带来的重复拦截时，仍保留同来源下载凭据、物理目标和明确缺失文件检查。新增只读接口不能触发下载、登记或写库。

## 验证状态

本轮代码修订为 `6f794fda227d194a752f635689df95451c8e1582`，已推送 `codex/local-workbench-preview`，PR #19 保持草稿。已补合成分页、取消与迟到响应、双来源搜索、同来源凭据、已删除文件、清空历史、旧关系失效和原生权限回归。删除的测试对应已取消功能，不把旧功能存在当成通过条件；队列、输出保护、ZIP、来源和账户检查继续保留。

| 工程检查 | 本轮证据 |
| --- | --- |
| 前端逻辑、构建及 Chromium 界面回归 | [CI 34822799306](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34822799306) 通过：139 项逻辑测试、96 项界面测试；包含实际运行的排行回归。 |
| Rust 工作区及 Windows 执行契约 | [CI 34822799322](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34822799322) 两项任务全部通过，包含现有来源、下载、ZIP、迁移、重复目标与文件保护回归。 |
| Windows 桌面应用 | [CI 34822799314](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34822799314) 模块测试、凭据测试、原生接口、Clippy、Dev 程序构建和实际 Windows WebView 启动／重启验证全部通过；安装包步骤跳过。 |
| 页面截图 | 已检查同修订 CI 生成的作者更新、任意作者搜索、哔咔排行截图；操作区与列表未出现遮挡。图片均为合成数据或占位图，不是来源封面验收。 |
| 真实来源、真实漫画库与用户验收 | 本轮未执行，待用户集中验收。 |

回归中修复了旧下载文件移除后残留索引影响重新下载、JM 仅报告总数时的搜索结束条件、详情返回丢失搜索上下文和筛选误报作者全入库等问题。运行配置已纳入新排行界面用例，不能仅凭测试文件存在声称覆盖。界面样例压缩包 SHA-256：`35a8c6714227a848984642bde2803fb410c2b488994b348efbe60d2a5c52b94b`。

正式测试、编译与界面检查只在 CI 运行；本地执行编辑、格式化与差异检查。CI 通过、真实来源验证、用户验收分别记录，当前不可等同。

## 当前位置与后续路线

| 阶段 | 状态 | 剩余工作 |
| --- | --- | --- |
| 0：范围确认 | 已确认 | 取消项不恢复。 |
| 1：下载记录入库 | 代码与工程验证通过 | 验证同源各页状态、真实文件删除后状态。 |
| 2：搜索和统一筛选 | 代码与工程验证通过 | 集中验收完整结果与返回操作。 |
| 3：手动作者更新 | 代码与工程验证通过 | 用户真实双来源检查与手选下载。 |
| 4：来源排行 | 代码与工程验证通过 | JM/Pica 真实榜单验收。 |
| **4：大范围批量选书、界面收尾 ← 下一开发位置** | 未完成 | 完整筛选范围选书、超过每批 50 本的调度和必要设置／诊断收尾。 |
| 5：综合 V1 候选版 | 未开始综合验收 | 一次完成用户描述的检查作者→下载→自动入库→搜索新作者→筛选→下载流程。 |

阅读器和程序内更新仍只在 V1 后讨论。漫画关联、不喜欢、语言推断／补齐、分类书单／手机入库、PDF／CBZ、复杂断网恢复、云端常驻监控等已取消项不再列入后续。

## 集中验收建议

候选版交付后，由用户选择真实样本操作，不需要再次整理或关联旧漫画。

1. 作者更新：进入页面时只展示上次记录；点击检查后读两站，途中显示范围和进度。停止或来源失败不报全部完成；下次检查仍保留未选作品。
2. 任意作者：不关注也能搜索；完整读取后核对同来源已入库／未入库数量，切换筛选并进入详情返回，结果保留。
3. 手选一本未登记作品下载，ZIP 完整保存后同一来源收藏、详情、作者页面和榜单状态同步，无需重新扫库。另一站同本保持独立状态。
4. 从电脑移走一份本软件登记的测试作品，返回应用刷新后不再计为已入库；明确重新准备下载仍可用。普通旧文件无下载登记时不自动识别。
5. 发现页检查 JM 期数／类型和 Pica 周／日／月榜，核对来源排序、封面、筛选、详情返回和手选下载。

## 本轮交付状态

当前代码与全部必需 CI 已通过，真实作者检查、真实榜单和用户连续使用流程仍待验收。本轮提供独立 0.3.4 Dev 可执行文件，不安装、不启动、不替换正在运行的旧程序，也不生成安装包。先退出旧 Dev，再打开交付目录中的新程序即可使用此前应用数据。

交付目录：`MangaMonitor-Dev-20260914-6f794fd`，可执行文件名：`MangaMonitor Dev.exe`。FileVersion／ProductVersion 为 `0.3.4`，大小 20,238,336 字节。CI 的测试合并提交为 `43eb235f9cd0a159a304b55bfdfce96cac000cb6`，不是另一个开发分支。

- [CI Dev 程序工件](https://github.com/gouluanjiang/MangaMonitor-AI/actions/runs/34822799314/artifacts/10338633728)，已核对 GitHub 摘要与 ZIP CRC。
- ZIP SHA-256：`a1f9940097c128a94deeb3dd3f521a0cc1523c8c18b61a78b66b58003e9a9383`。
- EXE SHA-256：`b5c9a3440918f453332a151a1a8a821bafc1d8da7007a0ad536bfbc0defcb33c`。
- [草稿 PR #19](https://github.com/gouluanjiang/MangaMonitor-AI/pull/19) 的标题和说明已更新到当前简化后的范围、实际测试及剩余路线。

代码已推送到现有开发分支，PR #19 保持草稿且未合并，生产开关保持关闭。本报告的最终 CI 结果与交付校验补充留在本地主项目，随下一次必要代码推送一并提交，避免仅更新报告重复触发构建。接续开发先读当前范围及本报告，继续阶段 4 大范围批次调度和界面／设置收尾，不恢复已取消的关联、语言推断或不喜欢功能。
