# MangaMonitor V1 收尾报告与路线图 · 2026-09-17

**当前位置：V1 最终体验验收通过，V1 开发阶段收尾完成。** 主体功能均有实现；`49c40e8` 三套 CI 通过，修正版已单独交付并在用户恢复 Computer Use 后启动。用户于 2026-09-17 明确确认“v1最终体验验收通过”。本次确认对应当前构建 `49c40e843009ca968b595e0fa5009fc096b949cd`，交付程序仍为 0.3.4 Dev；功能里程碑与程序版本号分别记录。工程验证、实机检查和用户最终验收均已留有记录。

## 本轮进展

- 确认遗漏来自不同页面：上次只修复独立作者搜索／作者更新，而来源搜索及关注名单作者入口仍用通用关键词列表。本次共用同一作者字段规则，并保留明确独立的作品关键词搜索和编号／链接查询。此前问题查询的缓存数据只读回放确认：新旧作者入口分区一致，误下载样本已被排除出作者作品。详见 [本次修复报告](SOURCE_AUTHOR_ENTRY_FIX_2026-09-17.md)。
- 设置搜索已补验：“记住会话”只留下账号分类，清空恢复五个分类。用户只代填关键词，代理完成观察与清空检查。
- 找回 9 月 9 日已通过的网站收藏写入／网页刷新核对记录，避免重复要求已验收操作；不扩大为所有来源组合都已测试。
- 按用户要求删除的误下载 ZIP 已完成后续清理：程序正常刷新漫画库后，旧索引记录已移除，在线结果实际显示未入库。没有直接修改私人存储。
- 本机 Pica 来源搜索读完 12 条，正确分成 6 条作者作品与 6 条其他结果；全选仅 6 条，切换清空选择，普通关键词模式保留全部 12 条。Pica 关注名单重试后正常但为空。用户完成 JM 登录后，现有关注作者入口完整返回 15 条明确作者作品；全选 15，切换结果区清空选择，返回正常。
- 旧交接全文保存到历史档案，现行交接和范围文档只保留一个当前位置。既有未提交报告已先备份。

本轮交付：`Documents/Codex/MangaMonitor-Dev-20260917-49c40e8/mangamonitor-workbench-preview.exe`。没有生成安装包；用户恢复窗口操作后已启动该确切修正版。准确构建和哈希见 [修复报告](SOURCE_AUTHOR_ENTRY_FIX_2026-09-17.md)。

## 功能状态

| 功能 | 当前证据 |
| --- | --- |
| 两源登录、会话、收藏读取／排序／完整目录、封面运行内缓存 | 已有用户验收和相应回归。收藏写入／网站刷新有用户选定样本的验收，非所有组合的穷尽认证。 |
| JM／Pica 下载、ZIP 保存、同站下载登记、删除文件后重下 | 用户已验收；代理补充 ZIP 页数、CRC、哈希、状态同步和重启证据。 |
| 电脑库排序、文件状态筛选、文件位置和诊断 | 已实现并有实机证据；误下样本的正常索引移除和在线未入库显示现已确认。 |
| 手动作者更新、双来源分页、独立作者搜索 | `44da160` 的作者字段分区／统计／全选排除和限定真实查询通过。没有逐本证明作者完整书目。 |
| **来源搜索、关注名单作者入口** | **补修 `49c40e8`：149 条逻辑／115 条 Chromium／34 条原生 IPC 与三套 CI 通过；Pica 来源入口、作者全选排除及关键词模式实测通过。用户登录后的 JM 关注入口也已完成 15 条样本的完整读取、作者字段和选择清理检查。** |
| 完整作者全部入库提示、收藏／排行跨页同步 | 有合成集成回归；没有制造完整真实作者库或榜单样本来冒充实机通过。 |
| JM 每周必看、Pica 排行、完整范围批量选书 | 实现与工程检查已完成；完整分页后选书、最多 500 条队列和有界准备保留。 |
| 设置分类／关键词、版本、诊断复制、正常关闭重启 | 入口和状态已有实机证据，设置关键词在本轮补齐。 |

已知可恢复问题：两源本机关注名单初次读取曾显示繁忙，手动重试一次后恢复。原因未单独确定，本报告不将其标记为已修复；不影响后续作者入口检查结果。

## 路线图

```text
范围确认 → 下载登记与统一状态 → 常用页面和完整分页
  → 手动作者更新／任意作者搜索 → 两站排行／完整范围选书／设置诊断
  → V1 综合验收：补齐所有作者入口、CI 与独立 Dev 工件 已完成
  → Pica 来源实测与已删样本索引清理 已完成
  → JM 现有关注入口实测 已完成
  → V1 最终体验验收通过／开发收尾完成 ← 当前
  → 后续维护或单独选择扩展方向
  → 旧库辅助核对、阅读器、应用内更新：仍待讨论，尚未启动
```

正式套件和构建仅在 CI 跑；本机承担不重复的编辑、诊断、工件校验和已恢复的实机检查。本次补修的限定原生检查已完成；没有启动下载或改动关注。用户最终验收已经通过，不再要求重复整套验收、百位抽查或生成小版本安装包。

入库仍只依据对应来源成功下载记录加实际文件。旧库和跨站同一本不自动关联；JM 不含 English Manga。作者字段缺失／别名未确认的命中单独保留，不误称全部无关，也不算作已证实作者作品。身份匹配／关联、不喜欢、语言版本推断、自动替换、手机名单、分类书单等取消项不恢复。本次只登记最终验收与更新路线图；Dev 保持 0.3.4、PR #19 草稿未合并、生产关闭。发布、安装包或后续扩展另行安排。
